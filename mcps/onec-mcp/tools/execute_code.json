{
  "name": "execute_code",
  "description": "Выполнить произвольный BSL-код на сервере 1С. Код должен присвоить результат переменной Результат. Пример: Результат = Справочники.Номенклатура.НайтиПоКоду(\"000001\").Наименование;",
  "inputSchema": {
    "type": "object",
    "properties": {
      "code": {
        "type": "string",
        "description": "BSL-код для выполнения на сервере 1С. Должен присваивать результат переменной Результат."
      },
      "safeMode": {
        "type": "boolean",
        "description": "Безопасный режим (ограничивает доступ к ОС и файлам). По умолчанию true.",
        "default": true
      }
    },
    "required": [
      "code"
    ]
  }
}