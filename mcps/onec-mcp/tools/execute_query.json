{
  "name": "execute_query",
  "description": "Выполнить запрос на языке 1С и получить результат в виде таблицы. Используйте для проверки данных в регистрах, остатков, движений документов, анализа справочников.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "queryText": {
        "type": "string",
        "description": "Текст запроса на языке 1С"
      },
      "maxRows": {
        "type": "number",
        "description": "Максимальное количество строк результата (по умолчанию 100)",
        "default": 100
      }
    },
    "required": [
      "queryText"
    ]
  }
}