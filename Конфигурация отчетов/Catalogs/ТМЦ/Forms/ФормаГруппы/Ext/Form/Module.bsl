
&НаСервере
Процедура ОбновитьНаСервере()
	ТМЦ = Справочники.ТМЦ.ВыбратьИерархически(Объект.Ссылка);
	Пока ТМЦ.Следующий() Цикл
		Если ТМЦ.ЭтоГруппа = 0 ТОгда
			ТекТМЦ = ТМЦ.Ссылка.ПолучитьОбъект();
			ТекТМЦ.Принадлежность = Объект.Принадлежность;
			ТекТМЦ.Записать();
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура Обновить(Команда)
	ОбновитьНаСервере();
КонецПроцедуры
