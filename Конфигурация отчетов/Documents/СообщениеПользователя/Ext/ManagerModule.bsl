Процедура ЗаписатьСообщениеПользователя(ТекстСообщения, Отправитель,СсылкаНаПроект) Экспорт
	
	ДокСообщения = Документы.СообщениеПользователя.СоздатьДокумент();
	ДокСообщения.Дата = ТекущаяДата();
	ДокСообщения.ТекстСообщения = СокрЛП(ТекстСообщения);
	ДокСообщения.Отправитель = Отправитель;
	ДокСообщения.СсылкаНаПроект = СсылкаНаПроект;
	ДокСообщения.Записать(РежимЗаписиДокумента.Запись);
	
КонецПроцедуры
