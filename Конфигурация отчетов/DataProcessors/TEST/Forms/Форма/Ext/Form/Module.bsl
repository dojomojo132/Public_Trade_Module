&НаСервере
Процедура ОткрытьКартинкуНаСервере()
	ЗапуститьПриложение("E:\Файлы\report\test.jpg");
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьКартинку(Команда)
	ОткрытьКартинкуНаСервере();
КонецПроцедуры


&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    Если Параметры.Свойство("ПереданноеЗначение") Тогда
        Значение = Параметры.ПереданноеЗначение; // Получаем переданное значение
        Сообщить("Получено значение: " + Значение);
    КонецЕсли;
КонецПроцедуры