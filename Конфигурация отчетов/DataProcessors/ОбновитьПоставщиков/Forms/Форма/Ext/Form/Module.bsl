
&НаСервере
Процедура ОбновитьНаСервере()
	ПрихДок = Документы.ПриходнаяНакладная.Выбрать();
	Пока ПрихДок.Следующий() Цикл
		Накл = ПрихДок.ПолучитьОбъект();
		Если Накл.Поставщик.ОсновнойПоставщик.Пустая()=Ложь Тогда
			Накл.Поставщик = Накл.Поставщик.ОсновнойПоставщик;
		    Накл.Записать(РежимЗаписиДокумента.Проведение);

		КонецЕсли;	
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура Обновить(Команда)
	ОбновитьНаСервере();
КонецПроцедуры
