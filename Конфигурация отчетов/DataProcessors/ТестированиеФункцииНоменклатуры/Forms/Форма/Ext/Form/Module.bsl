
&НаСервере
Процедура ПроверитьЦенМассивНаСервере()
	Массив = Новый Массив;
	Для Каждого Данные Из Объект.ТЧ Цикл
		Массив.Добавить(Данные.Номенклатура);	
	КонецЦикла;                              
	
	АктуальныеЦены = Справочники.Номенклатура.ПолучитьЦенуТовара(Массив, ТекущаяДата());
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьЦенМассив(Команда)
	ПроверитьЦенМассивНаСервере();
КонецПроцедуры
