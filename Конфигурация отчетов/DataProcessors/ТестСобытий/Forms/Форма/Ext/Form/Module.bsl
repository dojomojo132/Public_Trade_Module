
&НаКлиенте                         
Процедура ЗапускСобытий(Команда)   
	ЗапускСобытийНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗапускСобытийНаСервере()

	ПараметрыСобытия = Новый Структура("Источник", "ТестоваяОбработка");
    EventBus.ОпубликоватьСобытие(Справочники.События.РегистрацияЗаказа, ПараметрыСобытия);
	
КонецПроцедуры // ЗапускСобытийНаСервере()
