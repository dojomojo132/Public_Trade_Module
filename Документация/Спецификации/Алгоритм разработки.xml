<?xml version="1.0" encoding="UTF-8"?>
<DevelopmentAlgorithm xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    
    <Stage id="0" name="Защита данных">
        <Description>АВТОМАТИЧЕСКИ перед любыми изменениями. Двухуровневый бэкап: git (файлы конфигурации) + .dt (данные ИБ).</Description>
        <Actions>
            <Action id="0a" name="Git-бэкап">
                git add -A → git commit -m "BACKUP: дата перед описание задачи" → git push.
                Сохраняет ВСЕ файлы конфигурации (XML/BSL) + .dt в удалённый репозиторий.
                Формат коммита: "BACKUP: YYYY-MM-DD перед краткое описание задачи".
                Push ОБЯЗАТЕЛЕН — локальный коммит недостаточен.
            </Action>
            <Action id="0b" name="DT-бэкап">
                deploy-config.ps1 -Action Backup (или автоматически в -Action Full).
                Создаёт .dt копию текущей ИБ в папку backups/.
                Ротация: хранятся последние 5 бэкапов.
            </Action>
            <Action id="0c" name="Проверка предохранителей">
                Прочитать Документация/КРИТИЧЕСКИЕ_ОШИБКИ.md.
                Проверить ВСЕ правила-предохранители ПЕРЕД написанием кода.
                Не допускать повторения ранее зафиксированных критических ошибок.
            </Action>
            <Action id="0d" name="Синхронизация конфигурации (ОБЯЗАТЕЛЬНО!)">
                ПЕРЕД любыми изменениями файлов — выгрузить конфигурацию из ИБ:
                deploy-config.ps1 -Action Dump

                ЦЕЛЬ: гарантировать, что файлы на диске = конфигурация в ИБ.
                ИБ — единственный источник истины. Файлы могут отставать (изменения через Конфигуратор).

                Что делает Dump:
                1. Вызывает /DumpConfigToFiles → перезаписывает XML/BSL файлы из ИБ
                2. Обновляет ConfigDumpInfo.xml автоматически
                3. После Dump: файлы на диске гарантированно совпадают с ИБ

                ТОЛЬКО после успешного Dump — переходить к написанию кода.
            </Action>
        </Actions>
        <Rollback>
            <Action>При критических ошибках — двухэтапный откат:</Action>
            <Action>1. git checkout для восстановления файлов конфигурации (XML/BSL).</Action>
            <Action>2. deploy-config.ps1 -Action Rollback для восстановления ИБ из .dt.</Action>
            <Action>НИКОГДА не пересоздавать ИБ (CREATEINFOBASE) без разрешения пользователя.</Action>
            <Action>Если ошибка привела к нарушению целостности → ОБЯЗАТЕЛЬНО записать в Документация/КРИТИЧЕСКИЕ_ОШИБКИ.md.</Action>
        </Rollback>
    </Stage>

    <Stage id="1" name="Анализ требований">
        <Description>К этому этапу конфигурация уже выгружена из ИБ (ЭТАП 0d). Файлы на диске = ИБ.</Description>
        <Actions>
            <Action>Изучить входящий запрос пользователя.</Action>
            <Action>Проверить текущее состояние "ТЕХНИЧЕСКОЙ СПЕЦИФИКАЦИИ".</Action>
            <Action>Выявить конфликты с существующей логикой.</Action>
            <Action>Дополнительно: через MCP (mcp_mcp_1c_torgov_get_metadata_structure) убедиться в корректности структуры затрагиваемых объектов.</Action>
        </Actions>
    </Stage>

    <Stage id="2" name="Техническое задание">
        <Actions>
            <Action>Сформулировать список изменений (Архитектура, Метаданные, UI).</Action>
            <Action>Согласовать план работ с Пользователем.</Action>
        </Actions>
    </Stage>

    <Stage id="3" name="Документирование">
        <Description>Код не пишется, пока не обновлена Спецификация.</Description>
        <Actions>
            <Action>Открыть файл "ТЕХНИЧЕСКАЯ СПЕЦИФИКАЦИЯ КОНФИГУРАЦИИ PTM.txt".</Action>
            <Action>Внести изменения, используя шаблон из "Стандарт_описания_объектов.xml".</Action>
            <Validation>
                Убедиться, что заполнены блоки: Назначение, Модель данных, Логика.
            </Validation>
        </Actions>
    </Stage>

    <Stage id="4" name="Кодирование">
        <Actions>
            <Action>Внести изменения в метаданные Конфигуратора (строго по Спецификации).</Action>
            <Action>Написать программный код (Модули).</Action>
        </Actions>
    </Stage>

    <Stage id="5" name="Деплой и автоматическая отладка">
        <Description>deploy-config.ps1 -Action Full автоматически создаёт бэкап, загружает, проверяет, обновляет БД.</Description>
        <Actions>
            <Action>Запустить deploy-config.ps1 -Action Full.</Action>
            <Action>При ошибках — исправить и повторить деплой (цикл).</Action>
            <Action>Если ошибки не решаются за 2 попытки — ОТКАТИТЬ: deploy-config.ps1 -Action Rollback.</Action>
            <Action>После отката — исправить XML/BSL и повторить деплой заново.</Action>
        </Actions>
    </Stage>

    <Stage id="5.5" name="Начало сессии (ДИАЛОГ 3)">
        <Description>При запросе "начать сессию" / "запускай" / "старт" — стартовый диалог выбора действия.</Description>
        <Actions>
            <Action>ДИАЛОГ 3 (ask_questions) — стартовый выбор:
                1. "Мониторинг ошибок" → Сразу к ЭТАПУ 9 (Фаза мониторинга)
                2. "Новый запрос" + поле ввода → Текст запроса → Фаза 1 (полный цикл реализации)</Action>
        </Actions>
    </Stage>

    <Stage id="5.6" name="Интерактивная отладка (MCP: ptm-debug)">
        <Description>
            Использовать ТОЛЬКО при runtime-ошибках, которые не воспроизводятся через ТЖ/мониторинг,
            или когда требуется инспекция переменных в точке выполнения.
            Триггер: пользователь просит "отладить", "поставить breakpoint", "проверить переменные",
            либо после 2-х безуспешных попыток деплоя/мониторинга.
            Загрузить skill: 1c-debug (read_file .github/skills/1c-debug/SKILL.md).
        </Description>
        <Actions>
            <Action id="5.6a" name="Запуск отладчика">
                debug_connect — ВЫЗЫВАТЬ ПЕРВЫМ, запускает dbgs.exe и attaches.
                debug_launch — запускает 1С:Предприятие с отладкой.
            </Action>
            <Action id="5.6b" name="Установка точек останова">
                debug_set_breakpoints — { file_path: "полный путь к Module.bsl", lines: [N] }.
                По стеку из ТЖ определить проблемный модуль и строку.
            </Action>
            <Action id="5.6c" name="Инспекция">
                debug_get_stack — стек вызовов при остановке.
                debug_get_variables — локальные переменные текущего фрейма.
                debug_evaluate — { expression: "BSL_выражение" } — вычислить значение.
                debug_step_over / debug_step_into / debug_step_out — пошаговое выполнение.
            </Action>
            <Action id="5.6d" name="Завершение">
                debug_continue → продолжить.
                debug_disconnect — ОБЯЗАТЕЛЬНО в конце, иначе dbgs.exe остаётся запущенным.
            </Action>
            <Action id="5.6e" name="После отладки">
                Исправить BSL по найденной причине → вернуться к ЭТАПУ 5 (Деплой).
                Если ошибка системная → записать в Документация/КРИТИЧЕСКИЕ_ОШИБКИ.md.
            </Action>
        </Actions>
        <Notes>
            НИКОГДА не вызывать debug_evaluate без предварительной остановки на breakpoint.
            При зависании dbgs.exe: taskkill /f /im dbgs.exe.
            Конфиг: scripts/debug/debug_config.json.
        </Notes>
    </Stage>

    <Stage id="6" name="Непрерывный мониторинг">
        <Description>Запрос НЕ закрывается после деплоя. Автоцикл: мониторинг → фикс → деплой → мониторинг.</Description>
        <Actions>
            <Action>ЭТАП 9: Запустить monitor-errors.ps1 -Action Check -LastMinutes 5.</Action>
            <Action>ЭТАП 10: Если монитор нашёл ошибки — исправить → деплой → НАЗАД К ЭТАПУ 9.</Action>
            <Action>ЭТАП 11: ДИАЛОГ 1 (ask_questions) — три варианта:
                1. "Всё работает корректно" → ЭТАП 11а (ДИАЛОГ 2)
                2. "Есть ошибка, проверь журналы" → агент читает журналы, фиксит → деплой → ЭТАП 9
                3. "Нужно больше времени" → СТОП мониторинга</Action>
            <Action>ЭТАП 11а: ДИАЛОГ 2 (ask_questions) — режим нового запроса:
                1. "Остановить работу" → ЗАВЕРШЕНИЕ
                2. "Новый запрос" + поле ввода → ОЧИСТКА КОНТЕКСТА:
                   a) Сброс todo-листа (manage_todo_list с [])
                   b) Не ссылаться на предыдущий запрос (новая изолированная задача)
                   c) Заново проверить метаданные через MCP
                   d) Полный цикл Фаза 1-4 с чистого листа → ЭТАП 9</Action>
            <Action>ЭТАП 12: После любого фикса — деплой → ОБЯЗАТЕЛЬНО назад к ЭТАПУ 9.</Action>
        </Actions>
    </Stage>

    <Stage id="7" name="Подтверждение">
        <Description>Только после подтверждения пользователем.</Description>
        <Actions>
            <Action>
                Обновить блок "История изменений" (History) внутри Спецификации для затронутых объектов.
            </Action>
            <Action>Если в процессе работы была нарушена целостность — записать ошибку в Документация/КРИТИЧЕСКИЕ_ОШИБКИ.md (категория, симптом, причина, решение, правило-предохранитель).</Action>
            <Action>Выполнить коммит в репозиторий.</Action>
            <Action>Перевести задачу в статус "ВЫПОЛНЕНО".</Action>
        </Actions>
    </Stage>

</DevelopmentAlgorithm>