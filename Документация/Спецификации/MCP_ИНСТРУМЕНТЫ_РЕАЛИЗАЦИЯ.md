# Реализация дополнительных MCP-инструментов для PTM

> **Дата:** 2026-02-26  
> **Статус:** Реализовано (42 инструмента, 4 ресурса, 6 промптов)  
> **Основа:** Расширение [vladimir-kharin/1c_mcp](https://github.com/vladimir-kharin/1c_mcp)  
> **Версия расширения:** 1.2 (MCP_Сервер.cfe)

---

## 1. ТЕКУЩЕЕ СОСТОЯНИЕ

### 1.1 Установленные компоненты

| Компонент | Статус | Описание |
|-----------|--------|----------|
| `mcp_APIBackend` (HTTPСервис) | ✅ Установлен | HTTP-эндпоинт: `http://localhost/PTM/hs/mcp/mcp` |
| `mcp_ИнструментДанныеОКонфигурации` | ✅ Работает | `list_metadata_objects`, `get_metadata_structure` |
| `mcp_РесурсОписаниеСинтаксисаВстроенногоЯзыка` | ✅ Работает | Ресурс с описанием синтаксиса BSL |
| `mcp_УправлениеСервером` | ✅ Работает | Внутреннее управление MCP-сервером |
| `mcp_ОбщегоНазначения` | ✅ Работает | JSON-утилиты |
| `mcp_Метаданные` | ✅ Работает | Схемы параметров, таблицы инструментов |
| `mcp_Выполнение` | ✅ Работает | Вызов методов контейнеров |
| `mcp_КонтейнерыПовтИсп` | ✅ Работает | Кеширование таблиц |
| `mcp_Содержимое` | ✅ Работает | Формирование содержимого ответов |
| `mcp_ОсновнаяРоль` (Роль) | ✅ Установлен | Роль доступа к MCP |
| VS Code MCP-клиент | ✅ Настроен | `%APPDATA%\Code\User\mcp.json` → `mcp_1c_torgovly` |

### 1.2 Используемые инструменты (сейчас)

| Инструмент | Вызов из VS Code | Назначение |
|-----------|------------------|------------|
| `list_metadata_objects` | `mcp_mcp_1c_torgov_list_metadata_objects` | Список объектов метаданных по типу с фильтром |
| `get_metadata_structure` | `mcp_mcp_1c_torgov_get_metadata_structure` | Структура объекта (реквизиты, ТЧ, измерения, ресурсы) |

### 1.3 Архитектура расширения

```
VS Code Copilot Agent
    │
    ├─ mcp_mcp_1c_torgov_list_metadata_objects(metaType, nameMask, maxItems)
    ├─ mcp_mcp_1c_torgov_get_metadata_structure(metaType, name)
    │
    ▼
HTTP POST → http://localhost/PTM/hs/mcp/mcp
    │
    ▼
mcp_APIBackend (HTTP-сервис в расширении)
    │  JSON-RPC: {"method": "tools/call", "params": {"name": "...", "arguments": {...}}}
    │
    ├─ mcp_КонтейнерыПовтИсп → кешированные таблицы инструментов/ресурсов/промптов
    ├─ mcp_Метаданные → автообнаружение контейнеров через подсистемы
    ├─ mcp_Выполнение → вызов ВыполнитьИнструмент() у нужной обработки
    │
    ▼
Обработки-контейнеры (подсистема mcp_КонтейнерыИнструментов):
    ├── mcp_ИнструментДанныеОКонфигурации  ← list_metadata_objects, get_metadata_structure
    └── [НОВЫЕ ОБРАБОТКИ]                   ← наши дополнительные инструменты
```

### 1.4 Принцип добавления нового инструмента

1. Создать обработку с префиксом `mcp_Инструмент...`
2. Включить в подсистему `mcp_КонтейнерыИнструментов`
3. Реализовать в модуле менеджера:
   - `Процедура ДобавитьИнструменты(Инструменты) Экспорт` — описание и JSON-схема параметров
   - `Функция ВыполнитьИнструмент(ИмяИнструмента, Аргументы) Экспорт` — логика выполнения
4. **Никаких изменений в HTTP-сервисе или других модулях не требуется**
5. После добавления в расширение: загрузить `.cfe` → обновить конфигурацию ИБ

---

## 2. ПЛАН НОВЫХ ИНСТРУМЕНТОВ

### 2.1 Приоритет 1 — Критически полезно

#### 2.1.1 `execute_query` — Выполнение запроса 1С

**Зачем:** Проверка данных в регистрах, остатков, движений документов прямо из агента. Сейчас агент слеп — пишет код проведения, но не может проверить результат.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `queryText` | string | Да | Текст запроса на языке 1С |
| `params` | object | Нет | Параметры запроса (ключ=имя, значение=значение) |
| `maxRows` | number | Нет | Максимум строк результата (по умолчанию 100) |

**Примеры вызовов:**
```
// Проверить остатки товаров
execute_query(
  queryText: "ВЫБРАТЬ Номенклатура.Наименование, КоличествоОстаток 
               ИЗ РегистрНакопления.ОстаткиТоваров.Остатки()",
  maxRows: 50
)

// Проверить движения конкретного документа
execute_query(
  queryText: "ВЫБРАТЬ * ИЗ РегистрНакопления.ОстаткиТоваров 
               ГДЕ Регистратор = &Док",
  params: {"Док": "Документ.ЧекККМ.FindByNumber:00001"}
)

// Цены номенклатуры
execute_query(
  queryText: "ВЫБРАТЬ Номенклатура.Наименование, Цена, ТипЦены.Наименование 
               ИЗ РегистрСведений.ЦеныНоменклатуры.СрезПоследних()"
)
```

**Реализация BSL (модуль менеджера):**

```bsl
Процедура ДобавитьИнструменты(Инструменты) Экспорт
    
    МассивПараметров = Новый Массив;
    
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "queryText", "string", "Текст запроса на языке 1С", , Истина));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "maxRows", "number", "Максимальное количество строк результата", 100));
    
    СхемаПараметров = mcp_Метаданные.СхемаПараметровИнструмента(МассивПараметров);
    
    mcp_Метаданные.ДобавитьИнструмент(
        Инструменты,
        "execute_query",
        "Выполнение произвольного запроса 1С. Возвращает результат в текстовом виде (таблица). Используйте для проверки данных в регистрах, остатков, движений документов.",
        СхемаПараметров);
    
КонецПроцедуры

Функция ВыполнитьИнструмент(ИмяИнструмента, Аргументы) Экспорт
    
    Если ИмяИнструмента = "execute_query" Тогда
        Возврат ВыполнитьЗапрос(Аргументы);
    КонецЕсли;
    
    ВызватьИсключение "Неизвестный инструмент: " + ИмяИнструмента;
    
КонецФункции

Функция ВыполнитьЗапрос(Аргументы)
    
    ТекстЗапроса = Аргументы.queryText;
    Лимит = ?(Аргументы.Свойство("maxRows"), Аргументы.maxRows, 100);
    
    Запрос = Новый Запрос(ТекстЗапроса);
    
    // Установка параметров, если переданы
    Если Аргументы.Свойство("params") Тогда
        Для Каждого КлючЗначение Из Аргументы.params Цикл
            Запрос.УстановитьПараметр(КлючЗначение.Ключ, КлючЗначение.Значение);
        КонецЦикла;
    КонецЕсли;
    
    Результат = Запрос.Выполнить();
    
    Если Результат.Пустой() Тогда
        Возврат "Запрос вернул пустой результат (0 строк)";
    КонецЕсли;
    
    Выборка = Результат.Выбрать();
    Колонки = Результат.Колонки;
    
    // Формируем заголовок
    МассивКолонок = Новый Массив;
    Для Каждого Колонка Из Колонки Цикл
        МассивКолонок.Добавить(Колонка.Имя);
    КонецЦикла;
    
    СтрокиРезультата = Новый Массив;
    СтрокиРезультата.Добавить(СтрСоединить(МассивКолонок, " | "));
    СтрокиРезультата.Добавить(СтрСоединить(Новый Массив(МассивКолонок.Количество(), "---"), " | "));
    
    Счетчик = 0;
    Пока Выборка.Следующий() И Счетчик < Лимит Цикл
        МассивЗначений = Новый Массив;
        Для Каждого Колонка Из Колонки Цикл
            Значение = Выборка[Колонка.Имя];
            МассивЗначений.Добавить(?(Значение = Неопределено, "NULL", Строка(Значение)));
        КонецЦикла;
        СтрокиРезультата.Добавить(СтрСоединить(МассивЗначений, " | "));
        Счетчик = Счетчик + 1;
    КонецЦикла;
    
    Если Выборка.Количество() > Лимит Тогда
        СтрокиРезультата.Добавить("... ещё " + Строка(Выборка.Количество() - Лимит) + " строк (ограничение maxRows=" + Строка(Лимит) + ")");
    КонецЕсли;
    
    Возврат СтрСоединить(СтрокиРезультата, Символы.ПС);
    
КонецФункции
```

---

#### 2.1.2 `get_object_module` — Получение BSL-кода модуля из ИБ

**Зачем:** Сверка кода в ИБ с кодом на диске. Чтение модулей расширений (БПО), которых нет на диске. Анализ кода, который реально выполняется.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `metaType` | string | Да | Тип объекта (Catalogs, Documents, DataProcessors, CommonModules) |
| `name` | string | Да | Имя объекта |
| `moduleType` | string | Нет | Тип модуля: ObjectModule, ManagerModule, FormModule (по умолчанию ObjectModule) |
| `formName` | string | Нет | Имя формы (если moduleType=FormModule) |

**Реализация BSL:**

```bsl
Процедура ДобавитьИнструменты(Инструменты) Экспорт
    
    МассивПараметров = Новый Массив;
    
    СписокТипов = "Catalogs,Documents,DataProcessors,Reports,CommonModules,InformationRegisters,AccumulationRegisters";
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "metaType", "string", "Тип объекта метаданных", , Истина, СписокТипов));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "name", "string", "Имя объекта метаданных", , Истина));
    
    СписокТиповМодулей = "ObjectModule,ManagerModule,FormModule,RecordSetModule,CommandModule";
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "moduleType", "string", "Тип модуля", "ObjectModule", , СписокТиповМодулей));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "formName", "string", "Имя формы (если moduleType=FormModule)"));
    
    СхемаПараметров = mcp_Метаданные.СхемаПараметровИнструмента(МассивПараметров);
    
    mcp_Метаданные.ДобавитьИнструмент(
        Инструменты,
        "get_object_module",
        "Получить текст BSL-модуля объекта метаданных из информационной базы. Включая модули расширений.",
        СхемаПараметров);
    
КонецПроцедуры

Функция ВыполнитьИнструмент(ИмяИнструмента, Аргументы) Экспорт
    
    Если ИмяИнструмента = "get_object_module" Тогда
        Возврат ПолучитьТекстМодуля(Аргументы);
    КонецЕсли;
    
    ВызватьИсключение "Неизвестный инструмент: " + ИмяИнструмента;
    
КонецФункции

Функция ПолучитьТекстМодуля(Аргументы)
    
    МетаТип = Аргументы.metaType;
    Имя = Аргументы.name;
    ТипМодуля = ?(Аргументы.Свойство("moduleType"), Аргументы.moduleType, "ObjectModule");
    
    // Получаем объект метаданных
    Попытка
        КоллекцияМД = Метаданные[МетаТип];
    Исключение
        ВызватьИсключение "Неизвестный тип метаданных: " + МетаТип;
    КонецПопытки;
    
    МетаданныеОбъекта = КоллекцияМД.Найти(Имя);
    Если МетаданныеОбъекта = Неопределено Тогда
        ВызватьИсключение "Объект не найден: " + МетаТип + "." + Имя;
    КонецЕсли;
    
    // Для общих модулей — только один модуль
    Если МетаТип = "CommonModules" Тогда
        Возврат МетаданныеОбъекта.ПолучитьТекстМодуля();
    КонецЕсли;
    
    // Получаем нужный модуль
    Если ТипМодуля = "ObjectModule" Тогда
        Возврат МетаданныеОбъекта.ПолучитьТекстМодуляОбъекта();
    ИначеЕсли ТипМодуля = "ManagerModule" Тогда
        Возврат МетаданныеОбъекта.ПолучитьТекстМодуляМенеджера();
    ИначеЕсли ТипМодуля = "FormModule" Тогда
        ИмяФормы = ?(Аргументы.Свойство("formName"), Аргументы.formName, "");
        Если Не ЗначениеЗаполнено(ИмяФормы) Тогда
            ВызватьИсключение "Для FormModule необходимо указать параметр formName";
        КонецЕсли;
        Форма = МетаданныеОбъекта.Формы.Найти(ИмяФормы);
        Если Форма = Неопределено Тогда
            ВызватьИсключение "Форма не найдена: " + ИмяФормы;
        КонецЕсли;
        Возврат Форма.ПолучитьТекстМодуля();
    ИначеЕсли ТипМодуля = "RecordSetModule" Тогда
        Возврат МетаданныеОбъекта.ПолучитьТекстМодуляНабораЗаписей();
    КонецЕсли;
    
    ВызватьИсключение "Неизвестный тип модуля: " + ТипМодуля;
    
КонецФункции
```

---

#### 2.1.3 `execute_code` — Выполнение произвольного BSL-кода

**Зачем:** Тестирование логики, проверка формул, запуск исправлений данных, отладка без конфигуратора.

**⚠️ БЕЗОПАСНОСТЬ:** Выполнять в безопасном режиме по умолчанию. Добавить параметр `safeMode`.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `code` | string | Да | BSL-код для выполнения |
| `safeMode` | boolean | Нет | Безопасный режим (по умолчанию true) |

**Реализация BSL:**

```bsl
Процедура ДобавитьИнструменты(Инструменты) Экспорт
    
    МассивПараметров = Новый Массив;
    
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "code", "string", "BSL-код для выполнения на сервере 1С", , Истина));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "safeMode", "boolean", "Безопасный режим (ограничивает доступ к ОС, файлам). По умолчанию true.", Истина));
    
    СхемаПараметров = mcp_Метаданные.СхемаПараметровИнструмента(МассивПараметров);
    
    mcp_Метаданные.ДобавитьИнструмент(
        Инструменты,
        "execute_code",
        "Выполнить произвольный BSL-код на сервере 1С. Код должен присвоить результат переменной Результат. Возвращает строковое представление Результат.",
        СхемаПараметров);
    
КонецПроцедуры

Функция ВыполнитьИнструмент(ИмяИнструмента, Аргументы) Экспорт
    
    Если ИмяИнструмента = "execute_code" Тогда
        Возврат ВыполнитьКод(Аргументы);
    КонецЕсли;
    
    ВызватьИсключение "Неизвестный инструмент: " + ИмяИнструмента;
    
КонецФункции

Функция ВыполнитьКод(Аргументы)
    
    Код = Аргументы.code;
    БезопасныйРежим = ?(Аргументы.Свойство("safeMode"), Аргументы.safeMode, Истина);
    
    Если БезопасныйРежим Тогда
        УстановитьБезопасныйРежим(Истина);
    КонецЕсли;
    
    Результат = Неопределено;
    
    Попытка
        Выполнить(Код);
    Исключение
        ВызватьИсключение "Ошибка выполнения кода: " + ОписаниеОшибки();
    КонецПопытки;
    
    Если Результат = Неопределено Тогда
        Возврат "Код выполнен успешно. Переменная Результат не была установлена.";
    КонецЕсли;
    
    Возврат Строка(Результат);
    
КонецФункции
```

---

#### 2.1.4 `get_register_data` — Чтение данных регистра

**Зачем:** Проверка остатков товаров, цен, кассовых смен — без написания запросов вручную.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `registerType` | string | Да | Тип: AccumulationRegisters, InformationRegisters |
| `name` | string | Да | Имя регистра |
| `mode` | string | Нет | Режим: Balance (остатки), Turnovers (обороты), SliceLast (срез последних), All (все записи). По умолчанию All |
| `period` | string | Нет | Дата среза (формат ISO) |
| `filter` | object | Нет | Фильтр (ключ=имя измерения, значение=значение) |
| `maxRows` | number | Нет | Максимум строк (по умолчанию 100) |

**Реализация BSL:**

```bsl
Процедура ДобавитьИнструменты(Инструменты) Экспорт
    
    МассивПараметров = Новый Массив;
    
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "registerType", "string", "Тип регистра", , Истина, "AccumulationRegisters,InformationRegisters"));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "name", "string", "Имя регистра", , Истина));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "mode", "string", "Режим чтения", "All", , "Balance,Turnovers,SliceLast,All"));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "period", "string", "Дата среза в формате ISO (ГГГГ-ММ-ДДТЧЧ:ММ:СС)"));
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрИнструмента(
        "maxRows", "number", "Максимальное количество строк результата", 100));
    
    СхемаПараметров = mcp_Метаданные.СхемаПараметровИнструмента(МассивПараметров);
    
    mcp_Метаданные.ДобавитьИнструмент(
        Инструменты,
        "get_register_data",
        "Чтение данных регистра накопления или сведений. Поддерживает режимы: Balance (остатки), Turnovers (обороты), SliceLast (срез последних), All (все записи).",
        СхемаПараметров);
    
КонецПроцедуры
```

---

#### 2.1.5 `get_document_movements` — Движения документа по регистрам

**Зачем:** Мониторинг проведения: проверить, что документ (ЧекККМ, ПриходТовара) создал правильные движения.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `documentType` | string | Да | Тип документа (например, "ЧекККМ") |
| `documentNumber` | string | Да | Номер документа |
| `maxRows` | number | Нет | Максимум строк на регистр (по умолчанию 50) |

**Реализация BSL:**

```bsl
Функция ПолучитьДвиженияДокумента(Аргументы)
    
    ТипДокумента = Аргументы.documentType;
    НомерДокумента = Аргументы.documentNumber;
    Лимит = ?(Аргументы.Свойство("maxRows"), Аргументы.maxRows, 50);
    
    // Найти документ по номеру
    МДДокумента = Метаданные.Документы.Найти(ТипДокумента);
    Если МДДокумента = Неопределено Тогда
        ВызватьИсключение "Тип документа не найден: " + ТипДокумента;
    КонецЕсли;
    
    Менеджер = Документы[ТипДокумента];
    ДокСсылка = Менеджер.НайтиПоНомеру(НомерДокумента);
    
    Если ДокСсылка.Пустая() Тогда
        ВызватьИсключение "Документ не найден: " + ТипДокумента + " №" + НомерДокумента;
    КонецЕсли;
    
    РезультатМассив = Новый Массив;
    РезультатМассив.Добавить("Документ: " + Строка(ДокСсылка) + " от " + Строка(ДокСсылка.Дата));
    РезультатМассив.Добавить("Проведён: " + ?(ДокСсылка.Проведен, "Да", "Нет"));
    РезультатМассив.Добавить("");
    
    // Обойти все регистры, в которых документ может делать движения
    Для Каждого РегМД Из МДДокумента.Движения Цикл
        
        Запрос = Новый Запрос;
        Запрос.Текст = "ВЫБРАТЬ ПЕРВЫЕ " + Строка(Лимит) + " * ИЗ " + РегМД.ПолноеИмя() + " ГДЕ Регистратор = &Док";
        Запрос.УстановитьПараметр("Док", ДокСсылка);
        
        Результат = Запрос.Выполнить();
        
        Если Не Результат.Пустой() Тогда
            РезультатМассив.Добавить("=== " + РегМД.ПолноеИмя() + " ===");
            
            Выборка = Результат.Выбрать();
            Колонки = Результат.Колонки;
            
            // Заголовок
            МассивКолонок = Новый Массив;
            Для Каждого Колонка Из Колонки Цикл
                Если Колонка.Имя <> "Регистратор" Тогда
                    МассивКолонок.Добавить(Колонка.Имя);
                КонецЕсли;
            КонецЦикла;
            РезультатМассив.Добавить(СтрСоединить(МассивКолонок, " | "));
            
            Пока Выборка.Следующий() Цикл
                МассивЗначений = Новый Массив;
                Для Каждого Колонка Из Колонки Цикл
                    Если Колонка.Имя <> "Регистратор" Тогда
                        Значение = Выборка[Колонка.Имя];
                        МассивЗначений.Добавить(?(Значение = Неопределено, "NULL", Строка(Значение)));
                    КонецЕсли;
                КонецЦикла;
                РезультатМассив.Добавить(СтрСоединить(МассивЗначений, " | "));
            КонецЦикла;
            
            РезультатМассив.Добавить("");
        КонецЕсли;
        
    КонецЦикла;
    
    Если РезультатМассив.Количество() = 3 Тогда
        РезультатМассив.Добавить("Движений не найдено.");
    КонецЕсли;
    
    Возврат СтрСоединить(РезультатМассив, Символы.ПС);
    
КонецФункции
```

---

### 2.2 Приоритет 2 — Заметно ускорят работу

#### 2.2.1 `list_enum_values` — Значения перечисления

**Зачем:** При написании кода часто нужно знать точные имена: `Перечисления.СтавкиНДС.Без_НДС` или `Перечисления.СтавкиНДС.БезНДС`?

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `name` | string | Да | Имя перечисления |

**Реализация BSL:**

```bsl
Функция СписокЗначенийПеречисления(Аргументы)
    
    Имя = Аргументы.name;
    
    МДПеречисления = Метаданные.Перечисления.Найти(Имя);
    Если МДПеречисления = Неопределено Тогда
        ВызватьИсключение "Перечисление не найдено: " + Имя;
    КонецЕсли;
    
    МассивСтрок = Новый Массив;
    МассивСтрок.Добавить("Перечисление: " + МДПеречисления.ПолноеИмя());
    МассивСтрок.Добавить("Синоним: " + МДПеречисления.Синоним);
    МассивСтрок.Добавить("");
    МассивСтрок.Добавить("Имя | Синоним");
    МассивСтрок.Добавить("--- | ---");
    
    Для Каждого ЗначениеМД Из МДПеречисления.ЗначенияПеречисления Цикл
        МассивСтрок.Добавить(ЗначениеМД.Имя + " | " + ЗначениеМД.Синоним);
    КонецЦикла;
    
    Возврат СтрСоединить(МассивСтрок, Символы.ПС);
    
КонецФункции
```

---

#### 2.2.2 `get_predefined_values` — Предопределённые элементы справочника

**Зачем:** Убедиться, что предопределённые элементы существуют, получить их список.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `metaType` | string | Да | Тип объекта (Catalogs) |
| `name` | string | Да | Имя справочника |

---

#### 2.2.3 `find_references` — Поиск использований объекта

**Зачем:** Рефакторинг: перед удалением/изменением объекта — проверить зависимости в ИБ.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `objectRef` | string | Да | Ссылка на объект (тип.имя или конкретная ссылка) |
| `maxResults` | number | Нет | Максимум результатов (по умолчанию 50) |

---

#### 2.2.4 `get_users_list` — Список пользователей ИБ

**Зачем:** Мониторинг безопасности. Проверка, что пользователи не потерялись после деплоя.

**Реализация BSL:**

```bsl
Функция СписокПользователей(Аргументы)
    
    Пользователи = ПользователиИнформационнойБазы.ПолучитьПользователей();
    
    МассивСтрок = Новый Массив;
    МассивСтрок.Добавить("Имя | ПолноеИмя | АутентификацияОС | АутентификацияСтандартная | Роли");
    МассивСтрок.Добавить("--- | --- | --- | --- | ---");
    
    Для Каждого Польз Из Пользователи Цикл
        СписокРолей = Новый Массив;
        Для Каждого Роль Из Польз.Роли Цикл
            СписокРолей.Добавить(Роль.Имя);
        КонецЦикла;
        
        МассивСтрок.Добавить(
            Польз.Имя + " | " + 
            Польз.ПолноеИмя + " | " + 
            ?(Польз.АутентификацияОС, "Да", "Нет") + " | " + 
            ?(Польз.АутентификацияСтандартная, "Да", "Нет") + " | " + 
            СтрСоединить(СписокРолей, ", ")
        );
    КонецЦикла;
    
    МассивСтрок.Добавить("");
    МассивСтрок.Добавить("Всего пользователей: " + Строка(Пользователи.Количество()));
    
    Возврат СтрСоединить(МассивСтрок, Символы.ПС);
    
КонецФункции
```

---

#### 2.2.5 `get_form_structure` — Структура формы из ИБ

**Зачем:** Получить список реквизитов формы, элементов, команд, обработчиков — без чтения XML.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `metaType` | string | Да | Тип объекта |
| `name` | string | Да | Имя объекта |
| `formName` | string | Да | Имя формы |

---

### 2.3 Приоритет 3 — Удобства и автоматизация

#### 2.3.1 `get_event_log` — Журнал регистрации

**Зачем:** Прямой доступ к ошибкам из ЖР без `monitor-errors.ps1`.

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `level` | string | Нет | Уровень: Error, Warning, Information, Note. По умолчанию Error |
| `lastMinutes` | number | Нет | За последние N минут (по умолчанию 30) |
| `maxRows` | number | Нет | Максимум записей (по умолчанию 50) |
| `eventFilter` | string | Нет | Фильтр по событию (подстрока) |

**Реализация BSL:**

```bsl
Функция ПрочитатьЖурналРегистрации(Аргументы)
    
    Уровень = ?(Аргументы.Свойство("level"), Аргументы.level, "Error");
    ЗаМинут = ?(Аргументы.Свойство("lastMinutes"), Аргументы.lastMinutes, 30);
    Лимит = ?(Аргументы.Свойство("maxRows"), Аргументы.maxRows, 50);
    
    Отбор = Новый Структура;
    
    Если Уровень = "Error" Тогда
        Отбор.Вставить("Уровень", УровеньЖурналаРегистрации.Ошибка);
    ИначеЕсли Уровень = "Warning" Тогда
        Отбор.Вставить("Уровень", УровеньЖурналаРегистрации.Предупреждение);
    КонецЕсли;
    
    Отбор.Вставить("ДатаНачала", ТекущаяДатаСеанса() - ЗаМинут * 60);
    Отбор.Вставить("ДатаОкончания", ТекущаяДатаСеанса());
    
    СобытияЖР = Новый ТаблицаЗначений;
    ВыгрузитьЖурналРегистрации(СобытияЖР, Отбор, , , Лимит);
    
    Если СобытияЖР.Количество() = 0 Тогда
        Возврат "Записей в журнале регистрации не найдено (за последние " + Строка(ЗаМинут) + " мин, уровень: " + Уровень + ")";
    КонецЕсли;
    
    МассивСтрок = Новый Массив;
    МассивСтрок.Добавить("Журнал регистрации: " + Строка(СобытияЖР.Количество()) + " записей");
    МассивСтрок.Добавить("");
    
    Для Каждого Строка Из СобытияЖР Цикл
        МассивСтрок.Добавить("--- " + Строка(Строка.Дата) + " ---");
        МассивСтрок.Добавить("Событие: " + Строка(Строка.Событие));
        МассивСтрок.Добавить("Уровень: " + Строка(Строка.Уровень));
        МассивСтрок.Добавить("Комментарий: " + Строка(Строка.Комментарий));
        Если ЗначениеЗаполнено(Строка.Данные) Тогда
            МассивСтрок.Добавить("Данные: " + Строка(Строка.Данные));
        КонецЕсли;
        Если ЗначениеЗаполнено(Строка.Метаданные) Тогда
            МассивСтрок.Добавить("Метаданные: " + Строка(Строка.Метаданные));
        КонецЕсли;
        МассивСтрок.Добавить("");
    КонецЦикла;
    
    Возврат СтрСоединить(МассивСтрок, Символы.ПС);
    
КонецФункции
```

---

#### 2.3.2 `post_document` — Провести/отменить проведение документа

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `documentType` | string | Да | Тип документа |
| `documentNumber` | string | Да | Номер документа |
| `action` | string | Да | post (провести), unpost (отменить проведение) |

---

#### 2.3.3 `get_subsystem_content` — Состав подсистемы

**Параметры:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|-------------|----------|
| `name` | string | Да | Имя подсистемы |

---

### 2.4 Приоритет 4 — Ресурсы и промпты

#### 2.4.1 Ресурс: Техническая спецификация PTM

Предоставить агенту автоматический доступ к DataModel из нашей спецификации.

```bsl
Процедура ДобавитьРесурсы(ТаблицаРесурсов) Экспорт
    
    mcp_Метаданные.ДобавитьРесурс(
        ТаблицаРесурсов,
        "file://resource/ptm_datamodel.txt",
        "ptm_datamodel",
        "Описание структуры данных конфигурации PTM: справочники, документы, регистры, связи, бизнес-логика"
    );
    
КонецПроцедуры
```

#### 2.4.2 Ресурс: Стандарты кодирования 1С

```bsl
mcp_Метаданные.ДобавитьРесурс(
    ТаблицаРесурсов,
    "file://resource/1c_standards.txt",
    "1c_coding_standards",
    "Стандарты кодирования 1С для платформы 8.3.27: именование, запросы, транзакции, асинхронность"
);
```

#### 2.4.3 Промпт: Генерация обработки проведения

```bsl
Процедура ДобавитьПромпты(Промпты) Экспорт
    
    МассивПараметров = Новый Массив;
    МассивПараметров.Добавить(mcp_Метаданные.ПараметрПромпта(
        "documentName", "Имя документа (например, ПриходТовара)", Истина));
    
    mcp_Метаданные.ДобавитьПромпт(
        Промпты,
        "generate_posting_module",
        "Сгенерировать модуль проведения документа на основе его метаданных",
        mcp_Метаданные.ПараметрыПромпта(МассивПараметров)
    );
    
КонецПроцедуры
```

---

## 3. ГРУППИРОВКА ПО ОБРАБОТКАМ

Обработки на расширение — по принципу функциональной близости:

| Обработка | Инструменты | Подсистема |
|-----------|------------|------------|
| `mcp_ИнструментЗапросыИДанные` | `execute_query`, `get_register_data`, `get_document_movements` | `mcp_КонтейнерыИнструментов` |
| `mcp_ИнструментМодулиИКод` | `get_object_module`, `execute_code` | `mcp_КонтейнерыИнструментов` |
| `mcp_ИнструментСправочнаяИнформация` | `list_enum_values`, `get_predefined_values`, `get_form_structure`, `get_subsystem_content` | `mcp_КонтейнерыИнструментов` |
| `mcp_ИнструментАдминистрирование` | `get_users_list`, `get_event_log`, `post_document` | `mcp_КонтейнерыИнструментов` |
| `mcp_ИнструментПоискСсылок` | `find_references` | `mcp_КонтейнерыИнструментов` |
| `mcp_РесурсPTM` | DataModel PTM, Стандарты 1С | `mcp_КонтейнерыРесурсов` |
| `mcp_ПромптыPTM` | generate_posting_module | `mcp_КонтейнерыПромптов` |

---

## 4. ПОРЯДОК РЕАЛИЗАЦИИ

### Фаза 1 — Минимально жизнеспособный набор ✅ ЗАВЕРШЕНА (2026-02-24)

1. **`mcp_ИнструментЗапросыИДанные`** — `execute_query` + `get_register_data` + `get_document_movements` ✅
2. **`mcp_ИнструментСправочнаяИнформация`** — `list_enum_values` + `get_predefined_values` ✅

### Фаза 2 — Расширенная функциональность ✅ ЗАВЕРШЕНА (2026-02-25)

3. **`mcp_ИнструментМодулиИКод`** — `get_object_module` + `execute_code` ✅
   - `get_object_module` реализован как резолвер путей (safe mode расширения блокирует файловый доступ)
4. **`mcp_ИнструментАдминистрирование`** — `get_users_list` + `get_event_log` ✅

### Фаза 3 — Полный набор ✅ ЗАВЕРШЕНА (2026-02-25)

5. **`mcp_ИнструментСправочнаяИнформация`** — добавлены `get_form_structure` + `get_subsystem_content` ✅
   - `get_form_structure`: Возвращает список форм / структуру объекта-владельца + пути к файлам формы (метаданные формы не имеют .Реквизиты)
   - `get_subsystem_content`: Состав подсистемы + вложенные подсистемы (точка как разделитель вложенности)
6. **`mcp_ИнструментАдминистрирование`** — добавлен `post_document` ✅
   - Проведение/отмена проведения документа по типу+номеру
7. **`mcp_ИнструментПоискСсылок`** — `find_references` ✅
   - НайтиПоСсылкам() с поиском по коду/наименованию/номеру
8. Ресурсы и промпты — по мере необходимости

### Фаза 4 — Диагностика и аналитика

> **Обоснование:** Агент часто выполняет множество отдельных вызовов (list_metadata_objects для каждого типа, execute_query для проверки данных). Специализированные инструменты экономят до 5-10 шагов на рутинных операциях.

9. **`get_configuration_overview`** — обзор всей конфигурации одним вызовом
   - Возвращает: количество объектов каждого типа (справочники, документы, регистры, обработки, отчёты, перечисления, общие модули), основные подсистемы, версии и режимы
   - Заменяет: 8+ отдельных вызовов `list_metadata_objects` по каждому типу
   - Приоритет: P1 | Сложность: Низкая
   - Контейнер: `mcp_ИнструментДанныеОКонфигурации` (уже существует, добавить туда)

10. **`validate_metadata_integrity`** — post-deploy проверка целостности из ИБ
    - Параметры: `checkLevel` (quick/full)
    - Проверяет: все документы проводятся без исключений? все формы открываются? все запросы в модулях корректны?
    - Quick: только проверка метаданных (формы, модули, регистры). Full: + тестовое проведение документов.
    - Заменяет: ручной мониторинг через `monitor-errors.ps1` после деплоя
    - Приоритет: P1 | Сложность: Высокая
    - Контейнер: новый `mcp_ИнструментДиагностика`

11. **`get_connected_objects`** — граф зависимостей объекта метаданных
    - Параметры: `metaType`, `name`, `direction` (references/referenced_by/both)
    - Анализирует типы реквизитов/колонок ТЧ → показывает на какие объекты ссылается и кто ссылается на него
    - Заменяет: ручной анализ через `get_metadata_structure` + `find_references`
    - Приоритет: P2 | Сложность: Средняя
    - Контейнер: `mcp_ИнструментПоискСсылок` (добавить к find_references)

12. **`check_document_posting`** — диагностика проведения документа
    - Параметры: `documentType`, `documentNumber`
    - Проводит документ в транзакции → если ошибка, показывает где именно (стек, строка, модуль). Транзакция ОТКАТЫВАЕТСЯ.
    - Заменяет: `post_document` + `get_event_log` + ручной разбор стека
    - Приоритет: P2 | Сложность: Средняя
    - Контейнер: `mcp_ИнструментДиагностика`

13. **`analyze_bsl_antipatterns`** — поиск антипаттернов в BSL-модулях из ИБ
    - Параметры: `metaType`, `name` (опц.), `checksFilter` (опц.)
    - MCP-аналог `_ptm_analyze.py`: проверяет модули на ТекущаяДата(), модальные вызовы, ЭтаФорма и т.д.
    - Берёт код прямо из ИБ (через `execute_code` с чтением текста модуля), не с диска
    - Примечание: может потребовать обход safe mode (вызов через Выполнить)
    - Приоритет: P2 | Сложность: Высокая
    - Контейнер: `mcp_ИнструментДиагностика`

### Фаза 5 — Расширенное управление данными

> **Обоснование:** Агент часто создаёт тестовые данные через `execute_code` со сложными конструкциями. Специализированные инструменты упростят типичные сценарии.

14. **`create_catalog_item`** — создать элемент справочника
    - Параметры: `catalogName`, `name`, `code` (опц.), `parentName` (опц.), `fields` (JSON с доп. реквизитами)
    - Заменяет: multi-line `execute_code` с НовыйОбъект(), заполнением полей, Записать()
    - Приоритет: P2 | Сложность: Средняя
    - Контейнер: новый `mcp_ИнструментУправлениеДанными`

15. **`create_document`** — создать и (опционально) провести документ
    - Параметры: `documentType`, `date`, `fields` (JSON с реквизитами), `tabularSections` (JSON с ТЧ), `post` (булево)
    - Заменяет: execute_code с 20+ строками заполнения документа
    - Приоритет: P2 | Сложность: Средняя
    - Контейнер: `mcp_ИнструментУправлениеДанными`

16. **`update_register_record`** — создать/обновить запись регистра сведений
    - Параметры: `registerName`, `dimensions` (JSON), `resources` (JSON), `period` (опц.)
    - Заменяет: execute_code с МенеджерЗаписи, Период, Отбор, Записать()
    - Приоритет: P3 | Сложность: Средняя
    - Контейнер: `mcp_ИнструментУправлениеДанными`

### Фаза 6 — Ресурсы и промпты MCP ✅ РЕАЛИЗОВАНО

> **Обоснование:** MCP-протокол поддерживает не только tools, но и resources (контентные ресурсы) и prompts (шаблоны запросов). Это позволяет агенту получать контекст и шаблоны без дополнительных вызовов.

17. **MCP Resources** — контентные ресурсы, доступные через `resources/list` и `resources/read`
    - `ptm://datamodel` — динамическая модель данных конфигурации (справочники, документы, регистры, перечисления, константы с реквизитами и типами)
    - Реализация: обработка `mcp_РесурсPTM` генерирует контент из метаданных в runtime (безопасный режим!)
    - Контейнер: `mcp_КонтейнерыРесурсов`
    - Приоритет: P3 | Сложность: Средняя | **Дата: 2025-06-24**

18. **MCP Prompts** — шаблоны запросов через `prompts/list` и `prompts/get`
    - `generate_posting_module` — генерация шаблона модуля проведения документа (аргумент: documentName)
    - `create-metadata-object` — пошаговый промпт для создания нового объекта метаданных (аргументы: objectType, objectName, description)
    - `diagnose-posting-error` — промпт для диагностики ошибки проведения (аргументы: documentType, errorText)
    - Реализация: обработка `mcp_ПромптыPTM`, формирует контекстные MCP-сообщения с метаданными
    - Контейнер: `mcp_КонтейнерыПромптов`
    - Приоритет: P3 | Сложность: Низкая | **Дата: 2025-06-24**

> **Исправление в mcp_APIBackend:** `ПолучитьСписокПромптов` — заменён `JSONВСтруктуру` на прямое `ПрочитатьJSON` для корректного парсинга JSON-массивов параметров промптов.

---

### Анализ: что НЕ может стать MCP-инструментом

| Текущий скрипт | Причина | Статус |
|---|---|---|
| `validate-config.ps1` | Работает ДО загрузки в ИБ (проверяет XML на диске) | Останется скриптом |
| `deploy-config.ps1` | CLI-операции 1cv8.exe (LoadConfig, UpdateDB) | Останется скриптом |
| `monitor-errors.ps1` (ТЖ) | Читает файлы платформы на диске | Останется скриптом |
| `_load/_dump_mcp_extension.py` | CLI-операции 1cv8.exe | Останется скриптом |
| `_git_commit.py` | Git CLI | Останется скриптом |
| `run-tests.ps1` | BSL LS + YAxUnit — внешние инструменты | Останется скриптом |

---

## 5. ИНСТРУКЦИЯ ПО ДОБАВЛЕНИЮ ОБРАБОТКИ В РАСШИРЕНИЕ

### 5.1 Создание обработки (в конфигураторе)

1. Открыть расширение MCP_Сервер в конфигураторе
2. Добавить → Обработка → `mcp_ИнструментЗапросыИДанные`
3. Синоним: "Запросы и данные (инструмент)"
4. В модуле менеджера написать `ДобавитьИнструменты()` и `ВыполнитьИнструмент()`
5. Включить обработку в подсистему `mcp_КонтейнерыИнструментов`
6. Сохранить расширение → Обновить конфигурацию ИБ

### 5.2 Проверка работы

```powershell
# Проверить, что новый инструмент виден:
Invoke-RestMethod -Uri "http://localhost/PTM/hs/mcp/mcp" `
  -Method POST `
  -Body '{"jsonrpc":"2.0","method":"tools/list","id":1}' `
  -ContentType "application/json"

# Вызвать инструмент:
Invoke-RestMethod -Uri "http://localhost/PTM/hs/mcp/mcp" `
  -Method POST `
  -Body '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"execute_query","arguments":{"queryText":"ВЫБРАТЬ ПЕРВЫЕ 5 Наименование ИЗ Справочник.Номенклатура"}},"id":2}' `
  -ContentType "application/json"
```

### 5.3 Проверка из VS Code

После перезапуска VS Code (или переинициализации MCP) инструменты автоматически появятся:
- `mcp_mcp_1c_torgov_execute_query`
- `mcp_mcp_1c_torgov_get_register_data`
- и т.д.

---

## 6. СВОДНАЯ ТАБЛИЦА ВСЕХ ИНСТРУМЕНТОВ

> **Обновлено:** 2026-02-26 | **Итого:** 42 инструмента, 4 ресурса, 6 промптов

| # | Инструмент | Категория | Статус | Описание |
|---|-----------|-----------|--------|----------|
| ★ | `list_metadata_objects` | Встроен | ✅ | Список объектов метаданных по типу |
| ★ | `get_metadata_structure` | Встроен | ✅ | Структура объекта (реквизиты, ТЧ, типы) |
| 1 | `execute_query` | Phase 1 | ✅ | Выполнить запрос 1С, получить таблицу |
| 2 | `get_register_data` | Phase 1 | ✅ | Остатки/обороты/срез регистра |
| 3 | `get_document_movements` | Phase 1 | ✅ | Движения документа по регистрам |
| 4 | `list_enum_values` | Phase 1 | ✅ | Значения перечисления |
| 5 | `get_predefined_values` | Phase 1 | ✅ | Предопределённые элементы |
| 6 | `get_object_module` | Phase 2 | ✅ | Резолвер путей к BSL-модулям |
| 7 | `execute_code` | Phase 2 | ✅ | Произвольный BSL-код на сервере |
| 8 | `get_users_list` | Phase 2 | ✅ | Пользователи ИБ с ролями |
| 9 | `get_event_log` | Phase 2 | ✅ | Журнал регистрации с фильтрами |
| 10 | `find_references` | Phase 3 | ✅ | Поиск ссылок на объект в БД |
| 11 | `get_form_structure` | Phase 3 | ✅ | Список форм + структура + пути |
| 12 | `post_document` | Phase 3 | ✅ | Провести/отменить документ |
| 13 | `get_subsystem_content` | Phase 3 | ✅ | Состав подсистемы |
| 14 | `get_configuration_overview` | Phase 4 | ✅ | Обзор конфигурации одним вызовом |
| 15 | `validate_metadata_integrity` | Phase 4 | ✅ | Post-deploy проверка целостности |
| 16 | `get_connected_objects` | Phase 4 | ✅ | Граф зависимостей объекта |
| 17 | `check_document_posting` | Phase 4 | ✅ | Диагностика проведения |
| 18 | `analyze_module` | Phase 4 | ✅ | Анализ BSL-кода (процедуры, области) |
| 19 | `create_catalog_item` | Phase 5 | ✅ | Создать элемент справочника |
| 20 | `create_document` | Phase 5 | ✅ | Создать и провести документ |
| 21 | `update_register_record` | Phase 5 | ✅ | Запись в регистр сведений |
| 22 | `run_smoke_test` | Phase 5 | ✅ | Smoke-тест открытия форм |
| 23 | `get_data_summary` | Phase 5 | ✅ | Сводка записей по таблицам ИБ |
| 24 | `delete_object` | Cat A | ✅ | Пометка/удаление элемента |
| 25 | `update_catalog_item` | Cat A | ✅ | Обновить элемент справочника |
| 26 | `update_document` | Cat A | ✅ | Обновить реквизиты документа |
| 27 | `get_constant_value` | Cat A | ✅ | Прочитать значение константы |
| 28 | `set_constant_value` | Cat A | ✅ | Установить значение константы |
| 29 | `search_data` | Cat A | ✅ | Поиск по справочникам/документам |
| 30 | `get_rights_info` | Cat B | ✅ | Права и роли по объекту |
| 31 | `compare_periods` | Cat B | ✅ | Сравнение остатков/оборотов между периодами |
| 32 | `get_scheduled_jobs` | Cat B | ✅ | Список регламентных заданий |
| 33 | `run_report` | Cat B | ✅ | Формирование отчёта по имени |
| 34 | `bulk_create` | Cat B | ✅ | Пакетное создание элементов |
| 35 | `get_locks_info` | Cat B | ✅ | Информация о блокировках |
| 36 | `export_data` | Cat C | ✅ | Выгрузка данных JSON/CSV |
| 37 | `import_data` | Cat C | ✅ | Импорт данных из JSON (с updateExisting) |
| 38 | `get_session_info` | Cat C | ✅ | Информация о текущем сеансе |
| 39 | `clear_deleted` | Cat C | ✅ | Удаление помеченных (с проверкой ссылок) |
| 40 | `get_data_history` | Cat C | ✅ | История изменений из ЖР |
| 41 | `get_session_info` | Cat C | ✅ | Информация о сеансе |
| 42 | `analyze_module` | Cat C | ✅ | Анализ BSL-модулей |

### Ресурсы (resources/read)

| # | URI | Статус | Описание | Размер |
|---|-----|--------|----------|--------|
| 1 | `ptm://datamodel` | ✅ | Модель данных конфигурации | ~5000 симв. |
| 2 | `ptm://registers` | ✅ | Карта регистров с измерениями, ресурсами, регистраторами | ~9600 симв. |
| 3 | `ptm://business-logic` | ✅ | Документооборот: документы → реквизиты → ТЧ → движения | ~8600 симв. |
| 4 | `file://resource/syntax_1c.txt` | ✅ | Синтаксис встроенного языка 1С | — |

### Промпты (prompts/get)

| # | Имя | Статус | Аргументы | Описание |
|---|-----|--------|-----------|----------|
| 1 | `generate_posting_module` | ✅ | `documentName` | Шаблон модуля проведения |
| 2 | `create-metadata-object` | ✅ | `objectType`, `objectName`, `description` | Чеклист создания объекта |
| 3 | `diagnose-posting-error` | ✅ | `documentType`, `errorText` | Диагностика ошибки проведения |
| 4 | `generate_report_module` | ✅ | `reportName`, `mode` | Шаблон модуля отчёта (СКД/Программный) |
| 5 | `generate_form_handlers` | ✅ | `metaType`, `objectName` | Обработчики формы по структуре объекта |
| 6 | `diagnose_data_integrity` | ✅ | `registerName`, `symptom` | 6-шаговый план расследования целостности |
