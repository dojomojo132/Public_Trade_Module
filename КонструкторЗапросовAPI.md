# Конструктор запросов API — Проект

## 1. Структура метаданных

### Перечисление: ОператорыСравненияAPI

| Значение | Пользовательское имя | API-строка |
|----------|----------------------|------------|
| Равно | `=` | `==` |
| НеРавно | `≠` | `!=` |
| Больше | `>` | `=gt=` |
| Меньше | `<` | `=lt=` |
| БольшеИлиРавно | `≥` | `=ge=` |
| МеньшеИлиРавно | `≤` | `=le=` |
| Содержит | `содержит` | `=like=` |

### Перечисление: ДействиеПостОбработки

| Значение | Описание |
|----------|----------|
| Оставить | Оставить записи где условие истинно (включающий фильтр) |
| Исключить | Убрать записи где условие истинно (исключающий фильтр) |
| ЗаменитьЗначение | Заменить значение поля (трансформация данных) |

### Справочник: ТаблицыAPI

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Наименование | Строка(150) | Название для пользователя |
| ИмяAPI | Строка(150) | Имя таблицы в API (для URL) |

### Справочник: ПоляТаблицAPI (подчинён ТаблицыAPI)

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Владелец | СправочникСсылка.ТаблицыAPI | Таблица-владелец |
| Наименование | Строка(150) | Название для пользователя |
| ИмяAPI | Строка(150) | Имя поля в API (для фильтра) |
| ТипДанных | Строка(50) | Тип данных поля (string/number/boolean/date) |

### Справочник: ЗапросыAPI (конструктор, с предопределёнными)

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Наименование | Строка(150) | Название запроса |
| Таблица | СправочникСсылка.ТаблицыAPI | Целевая таблица |
| Описание | Строка(500) | Пояснение для пользователя |

#### ТЧ «Фильтры»

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Поле | СправочникСсылка.ПоляТаблицAPI | Поле для фильтрации |
| Оператор | ПеречислениеСсылка.ОператорыСравненияAPI | Оператор сравнения |
| Значение | Строка(250) | Явное значение (если не параметр) |
| ЭтоПараметр | Булево | Истина = значение передаётся при вызове |
| ИмяПараметра | Строка(100) | Ключ параметра (для подстановки) |
| Активен | Булево | Вкл/выкл фильтр без удаления |

#### ТЧ «ПостОбработка» (фильтрация результатов на стороне 1С)

API возвращает массив структур. Эта ТЧ определяет правила фильтрации/обработки
полученных данных **после** получения ответа, уже внутри 1С.

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Поле | СправочникСсылка.ПоляТаблицAPI | Поле в структуре ответа |
| Действие | ПеречислениеСсылка.ДействиеПостОбработки | Что делать: Оставить / Исключить / ЗаменитьЗначение |
| Оператор | ПеречислениеСсылка.ОператорыСравненияAPI | Оператор сравнения (==, !=, >, <, содержит...) |
| Значение | Строка(250) | Значение для сравнения (явное) |
| ЭтоПараметр | Булево | Истина = значение берётся из входных параметров |
| ИмяПараметра | Строка(100) | Ключ параметра для подстановки |
| НовоеЗначение | Строка(250) | Новое значение (для действия ЗаменитьЗначение) |
| Активен | Булево | Вкл/выкл правило без удаления |
| Порядок | Число | Порядок применения (по возрастанию) |

**Логика работы:**
- `Оставить` — из массива остаются **только** записи, подходящие под условие
- `Исключить` — из массива **удаляются** записи, подходящие под условие
- `ЗаменитьЗначение` — в оставшихся записях значение поля заменяется на `НовоеЗначение`
- Правила применяются **последовательно** по полю `Порядок`

#### ТЧ «ВыходныеПоля» (проекция — какие поля отдать в результате)

После API-фильтров и пост-обработки из каждой записи выбираются только указанные поля.
Если ТЧ пустая — отдаются все поля как есть (без фильтрации колонок).

| Реквизит | Тип | Описание |
|----------|-----|----------|
| Поле | СправочникСсылка.ПоляТаблицAPI | Поле из ответа API |
| Псевдоним | Строка(150) | Имя поля в итоговом результате (если пусто — берётся ИмяAPI) |
| Порядок | Число | Порядок колонок в результате |
| Активен | Булево | Вкл/выкл поле без удаления |

**Логика работы:**
- Обходятся записи ТЧ по `Порядок` (по возрастанию)
- Из каждой структуры в массиве берётся значение по `Поле.ИмяAPI`
- Ключ в итоговой структуре = `Псевдоним` (или `Поле.ИмяAPI` если псевдоним пуст)
- Если ВыходныеПоля пусто → данные проходят без изменений

---

## 2. Пример предопределённых элементов

```
ЗапросыAPI:
  - ЗабронированныеСтолы     → reserved-tables?filter=removed==false;orderId!=0
  - СвободныеСтолы           → reserved-tables?filter=removed==false;orderId==0
  - ЗаказПоИД                → orders?filter=id=={ИДЗаказа}          // параметр
  - ТоварыПоКатегории         → products?filter=categoryId=={Категория}
```

---

## 3. Код общего модуля: КонструкторЗапросовAPI

```bsl
////////////////////////////////////////////////////////////////////////////////
// Общий модуль КонструкторЗапросовAPI
// Сервер, ВызовСервера
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Выполняет API-запрос по предопределённому элементу справочника ЗапросыAPI.
//
// Параметры:
//  Токен              - Строка - Bearer-токен авторизации
//  Адрес              - Строка - адрес сервера API
//  ИмяЗапроса         - Строка - имя предопределённого элемента ЗапросыAPI
//  ПараметрыФильтра   - Структура - ключ = ИмяПараметра, значение = подставляемое значение
//                       Необязательный. Передаётся когда в фильтрах есть параметры (ЭтоПараметр = Истина).
//  МодификацияЗапроса - Булево - использовать HTTPS (порт 443). По умолчанию Ложь.
//
// Возвращаемое значение:
//  Структура, Массив, Ложь - результат запроса или Ложь при ошибке.
//
&НаСервере
Функция ВыполнитьЗапросAPI(Токен, Адрес, ИмяЗапроса, ПараметрыФильтра = Неопределено, МодификацияЗапроса = Ложь) Экспорт
    
    // Получаем элемент справочника по предопределённому имени
    ЗапросСсылка = Справочники.ЗапросыAPI[ИмяЗапроса];
    
    Если Не ЗначениеЗаполнено(ЗапросСсылка) Тогда
        ВызватьИсключение "Запрос API с именем """ + ИмяЗапроса + """ не найден";
    КонецЕсли;
    
    ЗапросОбъект = ЗапросСсылка.ПолучитьОбъект();
    
    // Получаем имя таблицы из связанного справочника
    ИмяТаблицы = ЗапросОбъект.Таблица.ИмяAPI;
    
    // Собираем строку фильтра
    СтрокаФильтра = СобратьСтрокуФильтра(ЗапросОбъект, ПараметрыФильтра);
    
    // Вызываем существующую функцию
    РезультатAPI = СделатьЗапросСФильтром(Токен, Адрес, МодификацияЗапроса, ИмяТаблицы, СтрокаФильтра);
    
    // Если запрос вернул ошибку — возвращаем как есть
    Если РезультатAPI = Ложь Тогда
        Возврат Ложь;
    КонецЕсли;
    
    // Применяем пост-обработку (фильтрация результатов на стороне 1С)
    Если ЗапросОбъект.ПостОбработка.Количество() > 0 Тогда
        РезультатAPI = ПрименитьПостОбработку(РезультатAPI, ЗапросОбъект, ПараметрыФильтра);
    КонецЕсли;
    
    // Применяем проекцию (выбираем только нужные поля)
    Если ЗапросОбъект.ВыходныеПоля.Количество() > 0 Тогда
        РезультатAPI = ПрименитьПроекцию(РезультатAPI, ЗапросОбъект);
    КонецЕсли;
    
    Возврат РезультатAPI;
    
КонецФункции

// Собирает строку фильтра по табличной части справочника ЗапросыAPI.
//
// Параметры:
//  ЗапросОбъект     - СправочникОбъект.ЗапросыAPI - объект запроса
//  ПараметрыФильтра - Структура, Неопределено - значения параметров для подстановки
//
// Возвращаемое значение:
//  Строка - готовая строка фильтра вида "field==value;field2!=value2"
//
&НаСервере
Функция СобратьСтрокуФильтра(ЗапросОбъект, ПараметрыФильтра = Неопределено) Экспорт
    
    ЧастиФильтра = Новый Массив;
    
    Для Каждого СтрокаФильтра Из ЗапросОбъект.Фильтры Цикл
        
        // Пропускаем неактивные фильтры
        Если Не СтрокаФильтра.Активен Тогда
            Продолжить;
        КонецЕсли;
        
        // Имя поля из справочника
        ИмяПоля = СтрокаФильтра.Поле.ИмяAPI;
        
        // Оператор
        СтрокаОператора = ПолучитьСтрокуОператора(СтрокаФильтра.Оператор);
        
        // Значение: явное или из параметров
        Если СтрокаФильтра.ЭтоПараметр Тогда
            Если ПараметрыФильтра = Неопределено Тогда
                ВызватьИсключение "Фильтр """ + ИмяПоля + """ требует параметр """
                    + СтрокаФильтра.ИмяПараметра + """, но параметры не переданы";
            КонецЕсли;
            
            Если Не ПараметрыФильтра.Свойство(СтрокаФильтра.ИмяПараметра) Тогда
                ВызватьИсключение "Не передан параметр """ + СтрокаФильтра.ИмяПараметра
                    + """ для фильтра """ + ИмяПоля + """";
            КонецЕсли;
            
            ЗначениеФильтра = Строка(ПараметрыФильтра[СтрокаФильтра.ИмяПараметра]);
        Иначе
            ЗначениеФильтра = СтрокаФильтра.Значение;
        КонецЕсли;
        
        // Собираем часть: field==value
        ЧастиФильтра.Добавить(ИмяПоля + СтрокаОператора + ЗначениеФильтра);
        
    КонецЦикла;
    
    // Соединяем через ";"
    Возврат СтрСоединить(ЧастиФильтра, ";");
    
КонецФункции

// Возвращает строковое представление оператора для API.
//
// Параметры:
//  Оператор - ПеречислениеСсылка.ОператорыСравненияAPI
//
// Возвращаемое значение:
//  Строка - "==", "!=", "=gt=", "=lt=", "=ge=", "=le=", "=like="
//
&НаСервереБезКонтекста
Функция ПолучитьСтрокуОператора(Оператор)
    
    Если Оператор = Перечисления.ОператорыСравненияAPI.Равно Тогда
        Возврат "==";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.НеРавно Тогда
        Возврат "!=";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Больше Тогда
        Возврат "=gt=";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Меньше Тогда
        Возврат "=lt=";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.БольшеИлиРавно Тогда
        Возврат "=ge=";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.МеньшеИлиРавно Тогда
        Возврат "=le=";
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Содержит Тогда
        Возврат "=like=";
    Иначе
        ВызватьИсключение "Неизвестный оператор сравнения";
    КонецЕсли;
    
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Применяет проекцию — оставляет только указанные поля из каждой записи.
// Поля берутся из ТЧ «ВыходныеПоля», сортируются по Порядку.
// Если ТЧ пустая или нет активных строк — данные возвращаются без изменений.
//
// Параметры:
//  Данные        - Массив из Структура - обработанный массив
//  ЗапросОбъект  - СправочникОбъект.ЗапросыAPI
//
// Возвращаемое значение:
//  Массив из Структура - массив с отобранными полями
//
&НаСервере
Функция ПрименитьПроекцию(Данные, ЗапросОбъект)
    
    // Если не массив — оборачиваем
    Если ТипЗнч(Данные) <> Тип("Массив") Тогда
        ОберткаМассив = Новый Массив;
        ОберткаМассив.Добавить(Данные);
        Данные = ОберткаМассив;
    КонецЕсли;
    
    // Собираем активные поля, сортируем по Порядку
    Поля = Новый Массив;
    Для Каждого Строка Из ЗапросОбъект.ВыходныеПоля Цикл
        Если Не Строка.Активен Тогда
            Продолжить;
        КонецЕсли;
        Поля.Добавить(Строка);
    КонецЦикла;
    
    // Если нет активных полей — отдаём как есть
    Если Поля.Количество() = 0 Тогда
        Возврат Данные;
    КонецЕсли;
    
    // Сортировка по Порядок
    Для и = 0 По Поля.Количество() - 2 Цикл
        Для к = и + 1 По Поля.Количество() - 1 Цикл
            Если Поля[и].Порядок > Поля[к].Порядок Тогда
                Временное = Поля[и];
                Поля[и] = Поля[к];
                Поля[к] = Временное;
            КонецЕсли;
        КонецЦикла;
    КонецЦикла;
    
    // Применяем проекцию к каждой записи
    Результат = Новый Массив;
    
    Для Каждого Запись Из Данные Цикл
        
        НоваяЗапись = Новый Структура;
        
        Для Каждого ОписаниеПоля Из Поля Цикл
            
            ИмяПоляAPI = ОписаниеПоля.Поле.ИмяAPI;
            
            // Имя в итоговой структуре: Псевдоним или ИмяAPI
            Если ЗначениеЗаполнено(ОписаниеПоля.Псевдоним) Тогда
                ИмяКлюча = ОписаниеПоля.Псевдоним;
            Иначе
                ИмяКлюча = ИмяПоляAPI;
            КонецЕсли;
            
            // Берём значение из исходной записи
            Если Запись.Свойство(ИмяПоляAPI) Тогда
                НоваяЗапись.Вставить(ИмяКлюча, Запись[ИмяПоляAPI]);
            Иначе
                НоваяЗапись.Вставить(ИмяКлюча, Неопределено);
            КонецЕсли;
            
        КонецЦикла;
        
        Результат.Добавить(НоваяЗапись);
        
    КонецЦикла;
    
    Возврат Результат;
    
КонецФункции

// Применяет правила пост-обработки к массиву результатов API.
// Правила из ТЧ «ПостОбработка» выполняются последовательно по полю Порядок.
//
// Параметры:
//  Данные           - Массив из Структура - результат API
//  ЗапросОбъект     - СправочникОбъект.ЗапросыAPI
//  ПараметрыФильтра - Структура, Неопределено - параметры (для пост-фильтров с ЭтоПараметр)
//
// Возвращаемое значение:
//  Массив из Структура - обработанный массив
//
&НаСервере
Функция ПрименитьПостОбработку(Данные, ЗапросОбъект, ПараметрыФильтра)
    
    // Если пришла не коллекция (одиночная структура) — оборачиваем в массив
    Если ТипЗнч(Данные) <> Тип("Массив") Тогда
        ОберткаМассив = Новый Массив;
        ОберткаМассив.Добавить(Данные);
        Данные = ОберткаМассив;
    КонецЕсли;
    
    // Собираем правила, сортируем по Порядку
    Правила = Новый Массив;
    Для Каждого Строка Из ЗапросОбъект.ПостОбработка Цикл
        Если Не Строка.Активен Тогда
            Продолжить;
        КонецЕсли;
        Правила.Добавить(Строка);
    КонецЦикла;
    
    // Сортировка по Порядок (простая пузырьковая, правил обычно мало)
    Для и = 0 По Правила.Количество() - 2 Цикл
        Для к = и + 1 По Правила.Количество() - 1 Цикл
            Если Правила[и].Порядок > Правила[к].Порядок Тогда
                Временное = Правила[и];
                Правила[и] = Правила[к];
                Правила[к] = Временное;
            КонецЕсли;
        КонецЦикла;
    КонецЦикла;
    
    // Применяем каждое правило последовательно
    Для Каждого Правило Из Правила Цикл
        
        ИмяПоля = Правило.Поле.ИмяAPI;
        
        // Определяем значение для сравнения
        Если Правило.ЭтоПараметр И ПараметрыФильтра <> Неопределено
            И ПараметрыФильтра.Свойство(Правило.ИмяПараметра) Тогда
            ЗначениеСравнения = Строка(ПараметрыФильтра[Правило.ИмяПараметра]);
        Иначе
            ЗначениеСравнения = Правило.Значение;
        КонецЕсли;
        
        Если Правило.Действие = Перечисления.ДействиеПостОбработки.Оставить Тогда
            Данные = ОтфильтроватьМассив(Данные, ИмяПоля, Правило.Оператор, ЗначениеСравнения, Истина);
            
        ИначеЕсли Правило.Действие = Перечисления.ДействиеПостОбработки.Исключить Тогда
            Данные = ОтфильтроватьМассив(Данные, ИмяПоля, Правило.Оператор, ЗначениеСравнения, Ложь);
            
        ИначеЕсли Правило.Действие = Перечисления.ДействиеПостОбработки.ЗаменитьЗначение Тогда
            Для Каждого Запись Из Данные Цикл
                Если Запись.Свойство(ИмяПоля) Тогда
                    Если СравнитьЗначения(Строка(Запись[ИмяПоля]), Правило.Оператор, ЗначениеСравнения) Тогда
                        Запись.Вставить(ИмяПоля, Правило.НовоеЗначение);
                    КонецЕсли;
                КонецЕсли;
            КонецЦикла;
            
        КонецЕсли;
        
    КонецЦикла;
    
    Возврат Данные;
    
КонецФункции

// Фильтрует массив структур по условию.
//
// Параметры:
//  МассивДанных       - Массив из Структура
//  ИмяПоля            - Строка - ключ в структуре
//  Оператор           - ПеречислениеСсылка.ОператорыСравненияAPI
//  ЗначениеСравнения  - Строка - с чем сравниваем
//  Оставить           - Булево - Истина = оставить подходящие, Ложь = убрать подходящие
//
// Возвращаемое значение:
//  Массив из Структура
//
&НаСервере
Функция ОтфильтроватьМассив(МассивДанных, ИмяПоля, Оператор, ЗначениеСравнения, Оставить)
    
    Результат = Новый Массив;
    
    Для Каждого Запись Из МассивДанных Цикл
        
        Если Не Запись.Свойство(ИмяПоля) Тогда
            // Поле отсутствует — пропускаем запись (включаем в результат)
            Если Оставить Тогда
                // Не соответствует условию → не оставляем
            Иначе
                // Не соответствует условию → не исключаем → оставляем
                Результат.Добавить(Запись);
            КонецЕсли;
            Продолжить;
        КонецЕсли;
        
        ЗначениеПоля = Строка(Запись[ИмяПоля]);
        Совпадение = СравнитьЗначения(ЗначениеПоля, Оператор, ЗначениеСравнения);
        
        Если Оставить И Совпадение Тогда
            Результат.Добавить(Запись);
        ИначеЕсли Не Оставить И Не Совпадение Тогда
            Результат.Добавить(Запись);
        КонецЕсли;
        
    КонецЦикла;
    
    Возврат Результат;
    
КонецФункции

// Сравнивает два строковых значения по оператору.
// Для числовых сравнений (>, <, >=, <=) пытается привести к числу.
//
// Параметры:
//  ЗначениеПоля      - Строка
//  Оператор           - ПеречислениеСсылка.ОператорыСравненияAPI
//  ЗначениеСравнения  - Строка
//
// Возвращаемое значение:
//  Булево
//
&НаСервереБезКонтекста
Функция СравнитьЗначения(ЗначениеПоля, Оператор, ЗначениеСравнения)
    
    Если Оператор = Перечисления.ОператорыСравненияAPI.Равно Тогда
        Возврат НРег(ЗначениеПоля) = НРег(ЗначениеСравнения);
        
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.НеРавно Тогда
        Возврат НРег(ЗначениеПоля) <> НРег(ЗначениеСравнения);
        
    ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Содержит Тогда
        Возврат СтрНайти(НРег(ЗначениеПоля), НРег(ЗначениеСравнения)) > 0;
        
    Иначе
        // Числовые сравнения
        Попытка
            ЧислоПоля = Число(ЗначениеПоля);
            ЧислоСравнения = Число(ЗначениеСравнения);
        Исключение
            // Не числа — сравниваем как строки
            Если Оператор = Перечисления.ОператорыСравненияAPI.Больше Тогда
                Возврат ЗначениеПоля > ЗначениеСравнения;
            ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Меньше Тогда
                Возврат ЗначениеПоля < ЗначениеСравнения;
            ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.БольшеИлиРавно Тогда
                Возврат ЗначениеПоля >= ЗначениеСравнения;
            ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.МеньшеИлиРавно Тогда
                Возврат ЗначениеПоля <= ЗначениеСравнения;
            КонецЕсли;
            Возврат Ложь;
        КонецПопытки;
        
        Если Оператор = Перечисления.ОператорыСравненияAPI.Больше Тогда
            Возврат ЧислоПоля > ЧислоСравнения;
        ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.Меньше Тогда
            Возврат ЧислоПоля < ЧислоСравнения;
        ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.БольшеИлиРавно Тогда
            Возврат ЧислоПоля >= ЧислоСравнения;
        ИначеЕсли Оператор = Перечисления.ОператорыСравненияAPI.МеньшеИлиРавно Тогда
            Возврат ЧислоПоля <= ЧислоСравнения;
        КонецЕсли;
        
    КонецЕсли;
    
    Возврат Ложь;
    
КонецФункции
//
// Параметры:
//  ТаблицаСсылка - СправочникСсылка.ТаблицыAPI
//
// Возвращаемое значение:
//  Массив из Структура - список запросов (Ссылка, Наименование, Описание)
//
&НаСервереБезКонтекста
Функция ПолучитьЗапросыПоТаблице(ТаблицаСсылка)
    
    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |   ЗапросыAPI.Ссылка КАК Ссылка,
    |   ЗапросыAPI.Наименование КАК Наименование,
    |   ЗапросыAPI.Описание КАК Описание
    |ИЗ
    |   Справочник.ЗапросыAPI КАК ЗапросыAPI
    |ГДЕ
    |   ЗапросыAPI.Таблица = &Таблица
    |   И НЕ ЗапросыAPI.ПометкаУдаления";
    Запрос.УстановитьПараметр("Таблица", ТаблицаСсылка);
    
    Результат = Новый Массив;
    Выборка = Запрос.Выполнить().Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Результат.Добавить(Новый Структура("Ссылка, Наименование, Описание",
            Выборка.Ссылка, Выборка.Наименование, Выборка.Описание));
    КонецЦикла;
    
    Возврат Результат;
    
КонецФункции

// Получает список параметров (ЭтоПараметр = Истина) для запроса.
// Собирает параметры из ОБЕИХ ТЧ: Фильтры (API) и ПостОбработка (1С).
//
// Параметры:
//  ЗапросСсылка - СправочникСсылка.ЗапросыAPI
//
// Возвращаемое значение:
//  Массив из Структура - (ИмяПараметра, ИмяПоляПользователя, ТипДанных, Источник)
//
&НаСервереБезКонтекста
Функция ПолучитьПараметрыЗапроса(ЗапросСсылка) Экспорт
    
    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |   Фильтры.ИмяПараметра КАК ИмяПараметра,
    |   Фильтры.Поле.Наименование КАК ИмяПоляПользователя,
    |   Фильтры.Поле.ТипДанных КАК ТипДанных,
    |   ""API"" КАК Источник
    |ИЗ
    |   Справочник.ЗапросыAPI.Фильтры КАК Фильтры
    |ГДЕ
    |   Фильтры.Ссылка = &Ссылка
    |   И Фильтры.ЭтоПараметр
    |   И Фильтры.Активен
    |
    |ОБЪЕДИНИТЬ ВСЕ
    |
    |ВЫБРАТЬ
    |   ПостОбработка.ИмяПараметра КАК ИмяПараметра,
    |   ПостОбработка.Поле.Наименование КАК ИмяПоляПользователя,
    |   ПостОбработка.Поле.ТипДанных КАК ТипДанных,
    |   ""ПостОбработка"" КАК Источник
    |ИЗ
    |   Справочник.ЗапросыAPI.ПостОбработка КАК ПостОбработка
    |ГДЕ
    |   ПостОбработка.Ссылка = &Ссылка
    |   И ПостОбработка.ЭтоПараметр
    |   И ПостОбработка.Активен";
    Запрос.УстановитьПараметр("Ссылка", ЗапросСсылка);
    
    Результат = Новый Массив;
    УжеДобавлены = Новый Соответствие; // Исключаем дубли по ИмяПараметра
    Выборка = Запрос.Выполнить().Выбрать();
    
    Пока Выборка.Следующий() Цикл
        Если УжеДобавлены[Выборка.ИмяПараметра] <> Неопределено Тогда
            Продолжить;
        КонецЕсли;
        УжеДобавлены.Вставить(Выборка.ИмяПараметра, Истина);
        Результат.Добавить(Новый Структура("ИмяПараметра, ИмяПоляПользователя, ТипДанных, Источник",
            Выборка.ИмяПараметра, Выборка.ИмяПоляПользователя, Выборка.ТипДанных, Выборка.Источник));
    КонецЦикла;
    
    Возврат Результат;
    
КонецФункции

#КонецОбласти
```

---

## 4. Пример использования на клиенте (модуль формы)

```bsl
////////////////////////////////////////////////////////////////////////////////
// Модуль формы — пример вызова конструктора запросов
////////////////////////////////////////////////////////////////////////////////

#Область ОбработчикиКомандФормы

&НаКлиенте
Асинх Процедура ВыполнитьЗапросНажатие(Команда)
    
    // 1. Определяем какой запрос выполнять (предопределённый элемент)
    ИмяЗапроса = "ЗаказПоИД";
    
    // 2. Получаем список параметров которые нужно запросить у пользователя
    ТребуемыеПараметры = КонструкторЗапросовAPI.ПолучитьПараметрыЗапроса(
        Справочники.ЗапросыAPI[ИмяЗапроса]);
    
    // 3. Запрашиваем значения у пользователя
    ПараметрыФильтра = Новый Структура;
    
    Для Каждого Параметр Из ТребуемыеПараметры Цикл
        
        ВведённоеЗначение = Ждать ВвестиСтрокуАсинх("",
            "Введите значение для: " + Параметр.ИмяПоляПользователя);
        
        Если ВведённоеЗначение = Неопределено Тогда
            Возврат; // Пользователь отменил
        КонецЕсли;
        
        ПараметрыФильтра.Вставить(Параметр.ИмяПараметра, ВведённоеЗначение);
        
    КонецЦикла;
    
    // 4. Выполняем запрос
    Результат = ВыполнитьЗапросНаСервере(ИмяЗапроса, ПараметрыФильтра);
    
    // 5. Обрабатываем результат
    Если Результат = Ложь Тогда
        Ждать ПредупреждениеАсинх("Ошибка выполнения запроса");
    Иначе
        // Заполнить таблицу на форме, вывести данные и т.д.
        ЗаполнитьТаблицуРезультатов(Результат);
    КонецЕсли;
    
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ВыполнитьЗапросНаСервере(ИмяЗапроса, ПараметрыФильтра)
    
    // Токен и Адрес — из настроек или констант
    Токен   = Константы.ТокенAPI.Получить();
    Адрес   = Константы.АдресСервераAPI.Получить();
    
    Возврат КонструкторЗапросовAPI.ВыполнитьЗапросAPI(
        Токен, Адрес, ИмяЗапроса, ПараметрыФильтра);
    
КонецФункции

&НаКлиенте
Процедура ЗаполнитьТаблицуРезультатов(Результат)
    
    // Логика заполнения таблицы формы данными из API
    // Зависит от структуры конкретного ответа
    
КонецПроцедуры

#КонецОбласти
```

---

## 5. Пример: как выглядит предопределённый элемент в данных

### Запрос «ЗабронированныеСтолы»

```
Справочник.ЗапросыAPI:
  Наименование:  Забронированные столы
  Таблица:       → ТаблицыAPI."reserved-tables"
  
  ТЧ Фильтры:
  ┌──────────────┬──────────┬──────────┬─────────────┬──────────────┬─────────┐
  │ Поле         │ Оператор │ Значение │ ЭтоПараметр │ ИмяПараметра │ Активен │
  ├──────────────┼──────────┼──────────┼─────────────┼──────────────┼─────────┤
  │ removed      │ Равно    │ false    │ Нет         │              │ Да      │
  │ orderId      │ НеРавно  │ 0        │ Нет         │              │ Да      │
  └──────────────┴──────────┴──────────┴─────────────┴──────────────┴─────────┘
  
  Результат фильтра → "removed==false;orderId!=0"
```

### Запрос «ЗаказПоИД» (с параметром)

```
Справочник.ЗапросыAPI:
  Наименование:  Заказ по ИД
  Таблица:       → ТаблицыAPI."orders"
  
  ТЧ Фильтры:
  ┌──────────────┬──────────┬──────────┬─────────────┬──────────────┬─────────┐
  │ Поле         │ Оператор │ Значение │ ЭтоПараметр │ ИмяПараметра │ Активен │
  ├──────────────┼──────────┼──────────┼─────────────┼──────────────┼─────────┤
  │ id           │ Равно    │          │ Да          │ ИДЗаказа     │ Да      │
  └──────────────┴──────────┴──────────┴─────────────┴──────────────┴─────────┘
  
  При вызове передаём: Новый Структура("ИДЗаказа", 12345)
  Результат фильтра → "id==12345"
```

### Запрос «АктивныеТовары» (с пост-обработкой)

```
Справочник.ЗапросыAPI:
  Наименование:  Активные товары дороже порога
  Таблица:       → ТаблицыAPI."products"
  
  ТЧ Фильтры (API-фильтр):
  ┌──────────────┬──────────┬──────────┬─────────────┬──────────────┬─────────┐
  │ Поле         │ Оператор │ Значение │ ЭтоПараметр │ ИмяПараметра │ Активен │
  ├──────────────┼──────────┼──────────┼─────────────┼──────────────┼─────────┤
  │ active       │ Равно    │ true     │ Нет         │              │ Да      │
  └──────────────┴──────────┴──────────┴─────────────┴──────────────┴─────────┘
  
  ТЧ ПостОбработка (фильтрация результатов в 1С):
  ┌──────────┬────────────┬──────────┬──────────┬─────────────┬──────────────┬──────────────┬─────────┬────────┐
  │ Действие │ Поле       │ Оператор │ Значение │ ЭтоПараметр │ ИмяПараметра │ НовоеЗначен. │ Активен │ Поряд. │
  ├──────────┼────────────┼──────────┼──────────┼─────────────┼──────────────┼──────────────┼─────────┼────────┤
  │ Оставить │ price      │ Больше   │          │ Да          │ МинЦена      │              │ Да      │ 1      │
  │ Исключить│ removed    │ Равно    │ true     │ Нет         │              │              │ Да      │ 2      │
  │ Заменить │ status     │ Равно    │ draft    │ Нет         │ черновик     │              │ Да      │ 3      │
  └──────────┴────────────┴──────────┴──────────┴─────────────┴──────────────┴──────────────┴─────────┴────────┘
  
  Поток: API → [{active: true, price: 50, removed: false, status: "draft"}, ...]
    → Пост-обработка 1: Оставить где price > МинЦена (параметр = 30)
    → Пост-обработка 2: Исключить где removed == true
    → Пост-обработка 3: Заменить status "draft" → "черновик"
  
  ТЧ ВыходныеПоля (проекция — что отдать):
  ┌──────────────┬───────────────┬────────┬─────────┐
  │ Поле         │ Псевдоним     │ Поряд. │ Активен │
  ├──────────────┼───────────────┼────────┼─────────┤
  │ customerName │ Клиент        │ 1      │ Да      │
  │ price        │ Цена          │ 2      │ Да      │
  │ status       │ Статус        │ 3      │ Да      │
  └──────────────┴───────────────┴────────┴─────────┘
  
  Итоговый поток:
    API → Фильтры → ПостОбработка → ВыходныеПоля
    [{active:true, price:50, removed:false, status:"черновик", customerName:"Иван"}]
    → [{Клиент:"Иван", Цена:50, Статус:"черновик"}]
```

---

## 6. Диаграмма вызова

```
Клиент                          Сервер
  │                                │
  │  ПолучитьПараметрыЗапроса()   │
  │ ──────────────────────────────>│  ← читает ТЧ Фильтры + ТЧ ПостОбработка
  │<────────────────────────────── │    где ЭтоПараметр = Истина
  │                                │  → [{ИмяПараметра, ИмяПоляПользователя}]
  │  ВвестиСтрокуАсинх() × N      │
  │  (запрашиваем значения)        │
  │                                │
  │  ВыполнитьЗапросAPI(           │
  │    Токен, Адрес,               │
  │    "ИмяПредопред",             │
  │    {Парам1: "знач1"})          │
  │ ──────────────────────────────>│
  │                                │  1. СобратьСтрокуФильтра()
  │                                │     → "field1==знач1;field2!=0"
  │                                │
  │                                │  2. СделатьЗапросСФильтром()
  │                                │     → HTTP GET /api/rest/table?filter=...
  │                                │     → Массив из Структура (сырые данные)
  │                                │
  │                                │  3. ПрименитьПостОбработку()
  │                                │     → Оставить/Исключить/Заменить
  │                                │     → Массив (очищенные данные)
  │                                │
  │                                │  4. ПрименитьПроекцию()
  │                                │     → Только нужные поля + псевдонимы
  │                                │     → Массив (финальный результат)
  │                                │
  │<────────────────────────────── │  → Финальный результат
  │                                │
  │  ЗаполнитьТаблицуРезультатов() │
  │                                │
```

---

## 7. Автозаполнение полей справочника из ответа API

### Сценарий использования

Пользователь открывает форму справочника `ТаблицыAPI`, вводит JSON-ответ от API
в многострочное поле (или вызывает запрос напрямую), и система автоматически создаёт
элементы `ПоляТаблицAPI` на основании ключей первой записи в массиве.

### Форма справочника ТаблицыAPI — дополнительные элементы

| Элемент формы | Тип | Описание |
|---------------|-----|----------|
| ОтветAPI | ТекстовоеПоле (многострочное) | Пользователь вставляет JSON-ответ из внешней программы (не сохраняется) |
| КомандаЗаполнитьПоля | Кнопка | Парсит JSON → создаёт ПоляТаблицAPI |

### Код модуля формы справочника ТаблицыAPI

```bsl
////////////////////////////////////////////////////////////////////////////////
// Модуль формы справочника ТаблицыAPI
// Автозаполнение подчинённого справочника ПоляТаблицAPI из JSON-ответа
////////////////////////////////////////////////////////////////////////////////

#Область ОписаниеПеременных
// Реквизиты формы (не сохраняемые):
// ОтветAPI - Строка - JSON-ответ API для парсинга
#КонецОбласти

#Область ОбработчикиКомандФормы

// Заполняет справочник ПоляТаблицAPI из JSON, введённого в поле ОтветAPI.
//
&НаКлиенте
Асинх Процедура ЗаполнитьПоляИзОтвета(Команда)
    
    Если Не ЗначениеЗаполнено(ОтветAPI) Тогда
        Ждать ПредупреждениеАсинх("Введите JSON-ответ API в поле ""Ответ API""");
        Возврат;
    КонецЕсли;
    
    Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
        Ждать ПредупреждениеАсинх("Сначала сохраните элемент справочника");
        Возврат;
    КонецЕсли;
    
    РезультатЗаполнения = ЗаполнитьПоляНаСервере(Объект.Ссылка, ОтветAPI);
    
    Если РезультатЗаполнения.Успешно Тогда
        Ждать ПредупреждениеАсинх("Создано полей: " + РезультатЗаполнения.Создано
            + ", пропущено (уже есть): " + РезультатЗаполнения.Пропущено);
    Иначе
        Ждать ПредупреждениеАсинх("Ошибка: " + РезультатЗаполнения.ОписаниеОшибки);
    КонецЕсли;
    
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Парсит JSON-ответ и создаёт элементы справочника ПоляТаблицAPI.
//
// Параметры:
//  ТаблицаСсылка - СправочникСсылка.ТаблицыAPI - владелец
//  ОтветJSON     - Строка - JSON строка (массив или объект)
//
// Возвращаемое значение:
//  Структура - (Успешно, Создано, Пропущено, ОписаниеОшибки)
//
&НаСервереБезКонтекста
Функция ЗаполнитьПоляНаСервере(ТаблицаСсылка, ОтветJSON)
    
    Результат = Новый Структура("Успешно, Создано, Пропущено, ОписаниеОшибки",
        Ложь, 0, 0, "");
    
    Попытка
        // Парсим JSON
        ЧтениеJSON = Новый ЧтениеJSON;
        ЧтениеJSON.УстановитьСтроку(ОтветJSON);
        Данные = ПрочитатьJSON(ЧтениеJSON);
        ЧтениеJSON.Закрыть();
    Исключение
        Результат.ОписаниеОшибки = "Ошибка парсинга JSON: " + ОписаниеОшибки();
        Возврат Результат;
    КонецПопытки;
    
    // Определяем первую запись для анализа ключей
    ПерваяЗапись = Неопределено;
    
    Если ТипЗнч(Данные) = Тип("Массив") И Данные.Количество() > 0 Тогда
        ПерваяЗапись = Данные[0];
    ИначеЕсли ТипЗнч(Данные) = Тип("Соответствие") Тогда
        ПерваяЗапись = Данные;
    ИначеЕсли ТипЗнч(Данные) = Тип("Структура") Тогда
        ПерваяЗапись = Данные;
    Иначе
        Результат.ОписаниеОшибки = "JSON должен содержать массив объектов или один объект";
        Возврат Результат;
    КонецЕсли;
    
    Если ПерваяЗапись = Неопределено Тогда
        Результат.ОписаниеОшибки = "Не удалось определить структуру ответа";
        Возврат Результат;
    КонецЕсли;
    
    // Получаем существующие поля чтобы не дублировать
    СуществующиеПоля = Новый Соответствие;
    Запрос = Новый Запрос;
    Запрос.Текст =
    "ВЫБРАТЬ
    |   ПоляТаблицAPI.ИмяAPI КАК ИмяAPI
    |ИЗ
    |   Справочник.ПоляТаблицAPI КАК ПоляТаблицAPI
    |ГДЕ
    |   ПоляТаблицAPI.Владелец = &Владелец
    |   И НЕ ПоляТаблицAPI.ПометкаУдаления";
    Запрос.УстановитьПараметр("Владелец", ТаблицаСсылка);
    Выборка = Запрос.Выполнить().Выбрать();
    Пока Выборка.Следующий() Цикл
        СуществующиеПоля.Вставить(НРег(Выборка.ИмяAPI), Истина);
    КонецЦикла;
    
    // Обходим ключи первой записи и создаём поля
    КоллекцияКлючей = Неопределено;
    Если ТипЗнч(ПерваяЗапись) = Тип("Соответствие") Тогда
        КоллекцияКлючей = ПерваяЗапись;
    ИначеЕсли ТипЗнч(ПерваяЗапись) = Тип("Структура") Тогда
        КоллекцияКлючей = ПерваяЗапись;
    КонецЕсли;
    
    Для Каждого КлючЗначение Из КоллекцияКлючей Цикл
        
        ИмяПоляAPI = Строка(КлючЗначение.Ключ);
        
        // Пропускаем если уже есть
        Если СуществующиеПоля[НРег(ИмяПоляAPI)] <> Неопределено Тогда
            Результат.Пропущено = Результат.Пропущено + 1;
            Продолжить;
        КонецЕсли;
        
        // Определяем тип данных по значению
        ТипДанных = ОпределитьТипДанных(КлючЗначение.Значение);
        
        // Создаём элемент справочника
        НовоеПоле = Справочники.ПоляТаблицAPI.СоздатьЭлемент();
        НовоеПоле.Владелец = ТаблицаСсылка;
        НовоеПоле.ИмяAPI = ИмяПоляAPI;
        НовоеПоле.Наименование = ПреобразоватьCamelCaseВЧитаемое(ИмяПоляAPI);
        НовоеПоле.ТипДанных = ТипДанных;
        НовоеПоле.Записать();
        
        Результат.Создано = Результат.Создано + 1;
        
    КонецЦикла;
    
    Результат.Успешно = Истина;
    Возврат Результат;
    
КонецФункции

// Определяет тип данных по значению из JSON.
//
// Параметры:
//  Значение - Произвольный - значение из распарсенного JSON
//
// Возвращаемое значение:
//  Строка - "string", "number", "boolean", "date", "object", "array"
//
&НаСервереБезКонтекста
Функция ОпределитьТипДанных(Значение)
    
    Если Значение = Неопределено Или Значение = Null Тогда
        Возврат "string";
    ИначеЕсли ТипЗнч(Значение) = Тип("Булево") Тогда
        Возврат "boolean";
    ИначеЕсли ТипЗнч(Значение) = Тип("Число") Тогда
        Возврат "number";
    ИначеЕсли ТипЗнч(Значение) = Тип("Строка") Тогда
        // Проверяем на дату (ISO формат)
        Если СтрДлина(Значение) >= 10
            И СтрНайти(Значение, "-") = 5
            И СтрНайти(Значение, "-", , 6) = 8 Тогда
            Возврат "date";
        КонецЕсли;
        Возврат "string";
    ИначеЕсли ТипЗнч(Значение) = Тип("Массив") Тогда
        Возврат "array";
    Иначе
        Возврат "object";
    КонецЕсли;
    
КонецФункции

// Преобразует camelCase / snake_case имя в читаемое для пользователя.
// Пример: "orderId" → "Order Id", "first_name" → "First Name"
//
// Параметры:
//  ИмяAPI - Строка
//
// Возвращаемое значение:
//  Строка - читаемое имя
//
&НаСервереБезКонтекста
Функция ПреобразоватьCamelCaseВЧитаемое(ИмяAPI)
    
    Результат = "";
    
    Для Позиция = 1 По СтрДлина(ИмяAPI) Цикл
        Символ = Сред(ИмяAPI, Позиция, 1);
        
        // snake_case: заменяем _ на пробел
        Если Символ = "_" Тогда
            Результат = Результат + " ";
            Продолжить;
        КонецЕсли;
        
        // camelCase: перед заглавной буквой вставляем пробел
        Если Символ = ВРег(Символ) И Символ <> НРег(Символ) И Позиция > 1 Тогда
            Результат = Результат + " ";
        КонецЕсли;
        
        // Первый символ — заглавный
        Если Позиция = 1 Тогда
            Результат = Результат + ВРег(Символ);
        Иначе
            Результат = Результат + Символ;
        КонецЕсли;
        
    КонецЦикла;
    
    Возврат СокрЛП(Результат);
    
КонецФункции

#КонецОбласти
```

### Пример работы

```
1. Открываем ТаблицыAPI → "Заказы" (ИмяAPI: "orders")

2. Делаем запрос в внешней программе, копируем JSON-ответ

3. Вставляем JSON в поле "Ответ API":
   [
     {"id": 123, "customerName": "Иван", "total": 500.50, "created": "2026-03-28", "active": true},
     {"id": 124, "customerName": "Пётр", "total": 1200.00, "created": "2026-03-27", "active": false}
   ]

4. Нажимаем "Заполнить поля" → система парсит первую запись:
   → Создаёт ПоляТаблицAPI:
   ┌─────────────────┬──────────────┬─────────────┬──────────┐
   │ Наименование     │ ИмяAPI       │ ТипДанных   │ Владелец │
   ├─────────────────┼──────────────┼─────────────┼──────────┤
   │ Id              │ id           │ number      │ Заказы   │
   │ Customer Name   │ customerName │ string      │ Заказы   │
   │ Total           │ total        │ number      │ Заказы   │
   │ Created         │ created      │ date        │ Заказы   │
   │ Active          │ active       │ boolean     │ Заказы   │
   └─────────────────┴──────────────┴─────────────┴──────────┘
   
   Сообщение: "Создано полей: 5, пропущено (уже есть): 0"
```

---
---

# РАБОЧАЯ ЗОНА — Поэтапная отладка

> Ниже — автономный код для тестирования каждого этапа конвейера.
> Работает без ИБ (справочники заменены на структуры).
> После отладки перенести логику в общий модуль.

---

## Этап 1: Фильтр (сборка строки фильтра для API)

**Задача:** из массива настроек фильтров собрать строку вида `removed==false;orderId!=0`

### Автономный код для отладки

```bsl
////////////////////////////////////////////////////////////////////////////////
// ЭТАП 1: ФИЛЬТР — автономная версия для тестирования
// Контекст: &НаСервере или обработка
////////////////////////////////////////////////////////////////////////////////

#Область Этап1_Фильтр

// Маппинг операторов — вместо перечисления
// Ключ = имя оператора, Значение = строка для API
//
&НаСервере
Функция ПолучитьМаппингОператоров()
    
    Операторы = Новый Соответствие;
    Операторы.Вставить("Равно",            "==");
    Операторы.Вставить("НеРавно",          "!=");
    Операторы.Вставить("Больше",           "=gt=");
    Операторы.Вставить("Меньше",           "=lt=");
    Операторы.Вставить("БольшеИлиРавно",   "=ge=");
    Операторы.Вставить("МеньшеИлиРавно",   "=le=");
    Операторы.Вставить("Содержит",         "=like=");
    
    Возврат Операторы;
    
КонецФункции

// Собирает строку фильтра из массива настроек.
//
// Параметры:
//  НастройкиФильтров - Массив из Структура:
//      * ИмяПоля       - Строка - имя поля API (например "removed", "orderId")
//      * Оператор      - Строка - ключ оператора ("Равно", "НеРавно", "Больше"...)
//      * Значение      - Строка - явное значение ("false", "0")
//      * ЭтоПараметр   - Булево - Истина = значение берётся из ПараметрыФильтра
//      * ИмяПараметра  - Строка - ключ в ПараметрыФильтра (если ЭтоПараметр)
//      * Активен       - Булево - Ложь = пропустить этот фильтр
//  ПараметрыФильтра - Структура, Неопределено - подставляемые значения
//
// Возвращаемое значение:
//  Строка - "removed==false;orderId!=0"
//
&НаСервере
Функция СобратьСтрокуФильтраАвтономно(НастройкиФильтров, ПараметрыФильтра = Неопределено) Экспорт
    
    Операторы = ПолучитьМаппингОператоров();
    ЧастиФильтра = Новый Массив;
    
    Для Каждого Фильтр Из НастройкиФильтров Цикл
        
        // Пропускаем неактивные
        Если Не Фильтр.Активен Тогда
            Продолжить;
        КонецЕсли;
        
        // Оператор
        СтрокаОператора = Операторы[Фильтр.Оператор];
        Если СтрокаОператора = Неопределено Тогда
            ВызватьИсключение "Неизвестный оператор: " + Фильтр.Оператор;
        КонецЕсли;
        
        // Значение: явное или параметр
        Если Фильтр.ЭтоПараметр Тогда
            
            Если ПараметрыФильтра = Неопределено Тогда
                ВызватьИсключение "Фильтр """ + Фильтр.ИмяПоля
                    + """ требует параметр """ + Фильтр.ИмяПараметра
                    + """, но параметры не переданы";
            КонецЕсли;
            
            Если Не ПараметрыФильтра.Свойство(Фильтр.ИмяПараметра) Тогда
                ВызватьИсключение "Не передан параметр """
                    + Фильтр.ИмяПараметра + """ для поля """ + Фильтр.ИмяПоля + """";
            КонецЕсли;
            
            ЗначениеФильтра = Строка(ПараметрыФильтра[Фильтр.ИмяПараметра]);
            
        Иначе
            ЗначениеФильтра = Фильтр.Значение;
        КонецЕсли;
        
        // Собираем: field==value
        ЧастиФильтра.Добавить(Фильтр.ИмяПоля + СтрокаОператора + ЗначениеФильтра);
        
    КонецЦикла;
    
    Возврат СтрСоединить(ЧастиФильтра, ";");
    
КонецФункции

// Создаёт одну запись настройки фильтра (удобный конструктор).
//
&НаСервере
Функция НовыйФильтр(ИмяПоля, Оператор, Значение = "", ЭтоПараметр = Ложь, ИмяПараметра = "", Активен = Истина)
    
    Возврат Новый Структура("ИмяПоля, Оператор, Значение, ЭтоПараметр, ИмяПараметра, Активен",
        ИмяПоля, Оператор, Значение, ЭтоПараметр, ИмяПараметра, Активен);
    
КонецФункции

#КонецОбласти
```

### Тест-кейсы

```bsl
////////////////////////////////////////////////////////////////////////////////
// ТЕСТЫ ЭТАПА 1: Фильтр
////////////////////////////////////////////////////////////////////////////////

#Область Тесты_Этап1

&НаСервере
Процедура ТестФильтр_ЯвныеЗначения()
    
    // Сценарий: Забронированные столы — removed==false;orderId!=0
    Фильтры = Новый Массив;
    Фильтры.Добавить(НовыйФильтр("removed",  "Равно",    "false"));
    Фильтры.Добавить(НовыйФильтр("orderId",  "НеРавно",  "0"));
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры);
    
    Ожидаемый = "removed==false;orderId!=0";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестЯвныеЗначения: ПРОЙДЕН");
        Сообщить("   Результат: " + Результат);
    Иначе
        Сообщить("❌ ТестЯвныеЗначения: ПРОВАЛЕН");
        Сообщить("   Ожидалось: " + Ожидаемый);
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ТестФильтр_СПараметрами()
    
    // Сценарий: Заказ по ИД — id=={ИДЗаказа}
    Фильтры = Новый Массив;
    Фильтры.Добавить(НовыйФильтр("id", "Равно", , Истина, "ИДЗаказа"));
    
    Параметры = Новый Структура("ИДЗаказа", 12345);
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры, Параметры);
    
    Ожидаемый = "id==12345";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестСПараметрами: ПРОЙДЕН");
        Сообщить("   Результат: " + Результат);
    Иначе
        Сообщить("❌ ТестСПараметрами: ПРОВАЛЕН");
        Сообщить("   Ожидалось: " + Ожидаемый);
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ТестФильтр_НеактивныйФильтр()
    
    // Сценарий: один фильтр активен, один нет
    Фильтры = Новый Массив;
    Фильтры.Добавить(НовыйФильтр("removed",  "Равно",   "false"));
    Фильтры.Добавить(НовыйФильтр("orderId",  "НеРавно", "0", , , Ложь)); // неактивен
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры);
    
    Ожидаемый = "removed==false";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестНеактивныйФильтр: ПРОЙДЕН");
        Сообщить("   Результат: " + Результат);
    Иначе
        Сообщить("❌ ТестНеактивныйФильтр: ПРОВАЛЕН");
        Сообщить("   Ожидалось: " + Ожидаемый);
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ТестФильтр_ВсеОператоры()
    
    // Сценарий: каждый оператор по очереди
    Фильтры = Новый Массив;
    Фильтры.Добавить(НовыйФильтр("a", "Равно",            "1"));
    Фильтры.Добавить(НовыйФильтр("b", "НеРавно",          "2"));
    Фильтры.Добавить(НовыйФильтр("c", "Больше",           "3"));
    Фильтры.Добавить(НовыйФильтр("d", "Меньше",           "4"));
    Фильтры.Добавить(НовыйФильтр("e", "БольшеИлиРавно",   "5"));
    Фильтры.Добавить(НовыйФильтр("f", "МеньшеИлиРавно",   "6"));
    Фильтры.Добавить(НовыйФильтр("g", "Содержит",         "test"));
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры);
    
    Ожидаемый = "a==1;b!=2;c=gt=3;d=lt=4;e=ge=5;f=le=6;g=like=test";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестВсеОператоры: ПРОЙДЕН");
        Сообщить("   Результат: " + Результат);
    Иначе
        Сообщить("❌ ТестВсеОператоры: ПРОВАЛЕН");
        Сообщить("   Ожидалось: " + Ожидаемый);
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ТестФильтр_ПустойСписок()
    
    // Сценарий: нет фильтров — пустая строка
    Фильтры = Новый Массив;
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры);
    
    Ожидаемый = "";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестПустойСписок: ПРОЙДЕН");
        Сообщить("   Результат: (пустая строка)");
    Иначе
        Сообщить("❌ ТестПустойСписок: ПРОВАЛЕН");
        Сообщить("   Ожидалось: (пустая строка)");
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ТестФильтр_СмешанныйСценарий()
    
    // Сценарий: комбо — явные + параметры + неактивные
    Фильтры = Новый Массив;
    Фильтры.Добавить(НовыйФильтр("active",      "Равно",       "true"));                          // явный
    Фильтры.Добавить(НовыйФильтр("categoryId",  "Равно",       , Истина, "Категория"));            // параметр
    Фильтры.Добавить(НовыйФильтр("removed",     "Равно",       "true", , , Ложь));                // неактивен
    Фильтры.Добавить(НовыйФильтр("price",       "БольшеИлиРавно", , Истина, "МинЦена"));          // параметр
    
    Параметры = Новый Структура;
    Параметры.Вставить("Категория", "drinks");
    Параметры.Вставить("МинЦена", 100);
    
    Результат = СобратьСтрокуФильтраАвтономно(Фильтры, Параметры);
    
    Ожидаемый = "active==true;categoryId==drinks;price=ge=100";
    Если Результат = Ожидаемый Тогда
        Сообщить("✅ ТестСмешанный: ПРОЙДЕН");
        Сообщить("   Результат: " + Результат);
    Иначе
        Сообщить("❌ ТестСмешанный: ПРОВАЛЕН");
        Сообщить("   Ожидалось: " + Ожидаемый);
        Сообщить("   Получено:  " + Результат);
    КонецЕсли;
    
КонецПроцедуры

// Запуск всех тестов фильтра
//
&НаСервере
Процедура ЗапуститьТестыЭтап1() Экспорт
    
    Сообщить("══════════════════════════════════════");
    Сообщить("  ЭТАП 1: ФИЛЬТР — тестирование");
    Сообщить("══════════════════════════════════════");
    
    ТестФильтр_ЯвныеЗначения();
    ТестФильтр_СПараметрами();
    ТестФильтр_НеактивныйФильтр();
    ТестФильтр_ВсеОператоры();
    ТестФильтр_ПустойСписок();
    ТестФильтр_СмешанныйСценарий();
    
    Сообщить("══════════════════════════════════════");
    Сообщить("  Готово");
    Сообщить("══════════════════════════════════════");
    
КонецПроцедуры

#КонецОбласти
```

### Как запустить

1. Создать внешнюю обработку (`.epf`) или обработку в конфигурации
2. Скопировать код из «Автономный код» + «Тест-кейсы» в модуль формы / модуль объекта
3. Вызвать `ЗапуститьТестыЭтап1()` — по кнопке или `ПриОткрытии`
4. Результат — 6 тестов в окне сообщений

### Ожидаемый вывод

```
══════════════════════════════════════
  ЭТАП 1: ФИЛЬТР — тестирование
══════════════════════════════════════
✅ ТестЯвныеЗначения: ПРОЙДЕН
   Результат: removed==false;orderId!=0
✅ ТестСПараметрами: ПРОЙДЕН
   Результат: id==12345
✅ ТестНеактивныйФильтр: ПРОЙДЕН
   Результат: removed==false
✅ ТестВсеОператоры: ПРОЙДЕН
   Результат: a==1;b!=2;c=gt=3;d=lt=4;e=ge=5;f=le=6;g=like=test
✅ ТестПустойСписок: ПРОЙДЕН
   Результат: (пустая строка)
✅ ТестСмешанный: ПРОЙДЕН
   Результат: active==true;categoryId==drinks;price=ge=100
══════════════════════════════════════
  Готово
══════════════════════════════════════
```
