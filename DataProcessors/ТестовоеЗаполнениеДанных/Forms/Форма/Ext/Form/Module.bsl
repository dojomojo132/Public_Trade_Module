// =================================================================================
// МОДУЛЬ ФОРМЫ: Тестовое заполнение данных
// Объединенная обработка для заполнения всех тестовых данных
// =================================================================================

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// Установка значений по умолчанию для номенклатуры
	Если КоличествоЭлементов = 0 Тогда
		КоличествоЭлементов = 200;
	КонецЕсли;
	
	Если КоличествоГрупп = 0 Тогда
		КоличествоГрупп = 7;
	КонецЕсли;
	
	Если КоличествоЭлементовВГруппе = 0 Тогда
		КоличествоЭлементовВГруппе = Окр(КоличествоЭлементов / КоличествоГрупп);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаполнитьНоменклатуру(Команда)
	
	Если КоличествоЭлементов <= 0 Тогда
		ПоказатьПредупреждение(, "Количество элементов должно быть больше 0!");
		Возврат;
	КонецЕсли;
	
	Если КоличествоГрупп <= 0 Тогда
		ПоказатьПредупреждение(, "Количество групп должно быть больше 0!");
		Возврат;
	КонецЕсли;
	
	Если КоличествоЭлементовВГруппе <= 0 Тогда
		ПоказатьПредупреждение(, "Количество элементов в группе должно быть больше 0!");
		Возврат;
	КонецЕсли;
	
	ЗаполнитьНоменклатуруНаСервере();
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьКассы(Команда)
	СоздатьКассыНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьЦены(Команда)
	ЗаполнитьЦеныНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьШтрихкоды(Команда)
	ЗаполнитьШтрихкодыНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура СоздатьОборудование(Команда)
	СоздатьОборудованиеНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьНастройки(Команда)
	ЗаполнитьНастройкиНаСервере();
КонецПроцедуры

#КонецОбласти

#Область СерверныеПроцедуры

&НаСервере
Процедура ЗаполнитьНоменклатуруНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.ЗаполнитьНоменклатуру(КоличествоЭлементов, КоличествоГрупп, КоличествоЭлементовВГруппе);
КонецПроцедуры

&НаСервере
Процедура СоздатьКассыНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.СоздатьКассы();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьЦеныНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.ЗаполнитьЦены();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьШтрихкодыНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.ЗаполнитьШтрихкоды();
КонецПроцедуры

&НаСервере
Процедура СоздатьОборудованиеНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.СоздатьОборудование();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьНастройкиНаСервере()
	ОбъектОбработки = РеквизитФормыВЗначение("Объект");
	ОбъектОбработки.ЗаполнитьНастройкиРаспределения();
КонецПроцедуры

#КонецОбласти
