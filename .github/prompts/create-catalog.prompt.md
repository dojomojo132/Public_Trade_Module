---
description: 'Создание нового справочника 1С: XML → Configuration → CDI → подсистема → форма → валидация'
mode: agent
tools:
  - mcp_mcp_1c_torgov/*
---

# Создание нового справочника 1С

## Входные данные
- Имя справочника: {{ИМЯ}}
- Реквизиты: {{РЕКВИЗИТЫ}}
- Табличные части: {{ТЧ}} (опционально)
- Подсистема: {{ПОДСИСТЕМА}}

## Алгоритм (выполнить ВСЕ шаги)

### Шаг 1: Проверка
1. Прочитать спецификацию `Документация/Спецификации/ТЕХНИЧЕСКАЯ СПЕЦИФИКАЦИЯ КОНФИГУРАЦИИ PTM (Public Trade Module).xml`
2. Убедиться что справочник описан в спецификации
3. Проверить что справочника ещё нет в `Конфигурация/Проверка/Configuration.xml`

### Шаг 2: Создание XML объекта
1. Взять шаблон `Документация/Шаблоны/template-catalog.xml`
2. Скопировать ВЕСЬ заголовок с xmlns (НЕ менять!)
3. Заменить плейсхолдеры `{{...}}` реальными значениями
4. Сгенерировать UUID для каждого элемента
5. Сохранить как `Конфигурация/Проверка/Catalogs/{{ИМЯ}}.xml`

### Шаг 3-5: Multi-file обновления
Следовать [чеклисту создания объекта](../instructions/xml-metadata.instructions.md) — секция "Multi-file чеклист: НОВЫЙ объект метаданных".

### Шаг 6: Форма (если нужна)
Использовать prompt `/create-form` для создания формы справочника.

### Шаг 7: Валидация
1. `get_errors` на созданный XML файл
2. `validate-config.ps1` → 0 ошибок
3. Исправить ВСЕ ошибки

### Шаг 8: Спецификация
Обновить затронутый блок + добавить `<Record date="YYYY-MM-DD">Создан справочник {{ИМЯ}}</Record>`
