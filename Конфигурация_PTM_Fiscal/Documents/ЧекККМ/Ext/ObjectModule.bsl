&После("ОбработкаПроведения")
Процедура Фскл_ПослеОбработкиПроведения(Отказ, РежимПроведения)
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	Фскл_ФискализацияСервер.ВыполнитьФискализацию(Ссылка, Отказ);
	
КонецПроцедуры
